# Memory Log — BuildSafe AI

This file is a **living record** of what has actually been done, kept in sync with reality — not a plan (that's `phases.md`). Update it every time a task is completed, a decision is made, or a file is meaningfully changed. Anyone (or any AI agent) picking up the project should be able to read this file top to bottom and know exactly where things stand without re-reading the whole codebase.

## How to Update This File

- Add an entry under the current phase every time you finish a checklist item from `phases.md`.
- Log decisions (not just tasks) — e.g., "chose FastAPI over NestJS because ML lives in Python."
- If something was started but not finished, say so explicitly (`IN PROGRESS`, not silently omitted).
- Never delete history — append. If something is reversed, add a new entry noting the reversal; don't erase the old one.
- This file was reset to a fresh baseline on 2026-07-18 — prior session-by-session history was folded into the reference docs it actually belongs in (`design.md` now fully specifies the frontend implementation; `prd.md`/`architecture.md`/`rules.md`/`phases.md` carry the product/scope decisions). Start logging new work below as it happens.

---

## Project Status Snapshot

| Field | Value |
|---|---|
| Current phase | Phase 0 complete; Phase 6 backend AND Phase 7 frontend are now wired together end-to-end (real API calls both ways), still with TEMP ML/rule-engine placeholders standing in for the real Phase 3-5 work |
| Last updated | 2026-07-19 |
| Overall status | `apps/backend` has real auth (JWT+bcrypt), real job/followup/assess/assessments/recommendations endpoints, Gemini-based category tagging + follow-up-question phrasing (LLM layer switched from Anthropic to Gemini — user's explicit choice), and a `GET /jobs` list endpoint, all backed by Postgres via SQLAlchemy async + Alembic. **`apps/frontend` is no longer running on scripted demo data** — `app/chat/page.tsx` was rewritten to drive the conversation via real API calls (create job → skill/urgency quick-replies → LLM-phrased structured follow-up quick-replies → assess → real RiskCard), and auth (`EmailCodeAuth.tsx`) now does real email+password register/login against the backend (the old fake passwordless 6-digit-code step is gone). Risk decisions themselves are still produced by TEMPORARY placeholder classifier + rule-engine modules, NOT real Phase 3-5 ML/rules — the LLM is never involved in the risk decision itself, only category tagging and follow-up-question wording (rules.md §4 compliant). Postgres+pgvector confirmed running locally, though **port 5432 has a real conflict on this machine — see Known Issues, this blocks the plain documented `uvicorn`/`docker-compose` dev flow until resolved.** Phase 1 is blocked on supervisor answers to `prd.md`'s open questions (later provisionally resolved without supervisor sign-off — see below). Local Turbopack dev server is broken on this machine (native-subprocess crash, `0xc0000142`) — use `next dev --webpack` until fixed, see Known Issues. |

## Phase Progress

### Phase 0 — Project Setup
- [x] Repo structure created (`apps/frontend`, `apps/backend`, `ml/` — `docs/` intentionally not yet created, see Known Issues)
- [x] Postgres + pgvector running locally — confirmed 2026-07-18: `docker compose up -d` brought up `buildsafe-postgres` (healthy), `SELECT * FROM pg_extension` shows `vector` 0.8.5 enabled
- [x] `.env.example` documented for both apps (`apps/backend/.env.example`, `apps/frontend/.env.example`, placeholders only)
- [x] Lint/format configured (ruff+black for backend via `pyproject.toml`; ESLint+Prettier for frontend)
- Status: **Complete.** Scaffolding done and QA-verified end-to-end (2026-07-18); Postgres+pgvector confirmed running on the user's machine same day. Exit check ("all 3 members can run frontend + backend locally and hit a health-check endpoint") PASSES. Remaining loose ends tracked in Known Issues (not blockers): `ml/data|notebooks|eval/` dirs created but empty (train scripts land Phase 3/4); `docs/` folder not created.

### Frontend UI Design (ahead of Phase 7, design-only — see `design.md`)
- [x] Auth (login/register) and the full task-intake → risk-assessment chat flow built as a real React app in `chat-ui/`, matching `design.md` exactly.
- Status: **Reference implementation complete and documented in `design.md`.** Not yet wired to a real backend (Phase 1-6 haven't started) and not yet the real `apps/web` per `architecture.md`.

### Phase 1 — Research & Scope Finalization
- [x] Dataset size target + expert-review process — **provisionally resolved (2026-07-19)**, no supervisor available. See Decisions Log and `prd.md` §7.
- [x] Acceptable false-negative tolerance — **provisionally resolved (2026-07-19)** at ≥95% recall on the two most severe classes. See `prd.md` §7.
- [x] Task category list — already locked in practice (implemented identically across `chatData.ts`, `Sidebar.tsx`, `srs.md`, `rules.md`); no new decision needed.
- Status: **Provisionally complete.** `phases.md`'s exit check ("`prd.md` open questions section is empty") is met in the literal sense — the section now has answers instead of open questions — but both answers are self-determined, not supervisor-approved. Flagged clearly in `prd.md` §7 and `phases.md` for the supervisor to confirm or override whenever available; nothing was deleted or hidden.

### Phase 2 — Dataset Creation
- [x] **200–300 hand-written seed examples — done 2026-07-29: 256 in `ml/data/seed_examples.json`.** All 9 categories (electrical 36, plumbing 32, carpentry 30, general 30, hvac 28, masonry/painting/roofing/tiling 25 each) and all 5 risk levels (1:62, 2:52, 3:53, 4:45, 5:44 — no resampling should be needed for the Phase 3 baseline). 21 examples carry a `followup_questions` entry.
- [x] **`ml/validate_dataset.py` written** — mechanically enforces every rule in `ml/data/README.md` (risk/label agreement, `risk_level: 5` ⇒ empty `suggested_ppe`, unanswered-safety-critical-follow-up ⇒ `risk_level: 5`, canonical question wording, enum validity, exact-duplicate detection, `tool_available` only when `tools_available` is empty). Run it after any dataset change. It caught a real wording bug in the pre-expansion data on its first run.
- Schema, taxonomies (hazards, PPE, professional categories) and — most importantly — the **follow-up scoping rules** are documented in `ml/data/README.md`. Those rules were derived iteratively from real mistakes caught in review (see the 2026-07-29 Decisions Log entries); they are the part most likely to be violated during the template-expansion step, which is exactly why the validator exists.
- **Deliberate near-duplicates:** three pairs share a base task and differ only in what `task_text` states (the `"install a ceiling fan"` set, two `"remove a wall between the kitchen and living room"` variants, two `"light switch cover plate"` variants). They teach the model that *stated information*, not task type, drives the follow-up and risk outcome. **Keep each pair in the same train/val/test split or they leak.**
- [x] **Template variation + weak-labeling — done 2026-07-29: 299 generated variants in `ml/data/generated_examples.json`, total 555.** Produced by `ml/generate_variations.py` (deterministic, re-runnable). Seeds keep stable `id`s (`seed-0001`…); every generated row carries `variant_of` + `generation_rule`, and lives in a separate file so seeds stay the authoritative hand-judged set.
  - Three weak-labeling rules, **none of which can lower a risk level** (`rules.md` §4.2, enforced by the validator): `WL-1a:rephrase` (256 — conversational framings; seeds are all bare imperatives but real chat input is hedged/question-shaped, so this is genuine robustness coverage), `WL-1b:room-substitution` (26 — risk-neutral rooms only, never bathroom/basement/loft/garage where the room *is* part of the hazard), `WL-2:strip-confirmation` (17 — removes a stated safety confirmation so the condition becomes unaddressed, forcing `risk_level` 5; each stripped text hand-written, not regex-derived, because it changes a safety label).
  - **De-escalation is deliberately not automated.** Injecting a confirmation (which lowers risk) is the direction where a wrong label is dangerous, so those stay hand-written seeds only.
- Validator extended to cover the generated file: checks each variant's label against its stated rule and rejects any variant scoring below its parent. Verified it actually fails by feeding it 6 deliberate violations (de-escalation, un-escalated WL-2, risk-5-with-PPE, orphan `variant_of`, unknown rule, label/level mismatch) — all 6 caught.
- **Two traps for the split step (`ml/data/README.md` documents both):** (1) group by `variant_of` — a variant shares nearly all its text with its parent, so a naive split puts the same task in train and test; (2) the three deliberate near-duplicate *seed* pairs need the same treatment. Also: report headline metrics on hand-written seeds only, or the eval measures the generator rather than the task.
- [~] **Label-quality review — substitute done 2026-07-29, genuine expert review still outstanding.** `ml/review_high_risk.py` audits all 555 examples against 11 risk floors derived from published standards (HSE, OSHA Focus Four, Gas Safety Regs 1998, Control of Asbestos Regs 2012, BS 7671/Part P, Confined Spaces Regs 1997) plus `srs.md` §9; write-up in `ml/data/REVIEW.md`. **95.8% → 100% conformance after fixes.**
  - ⚠️ **This is NOT an inter-rater agreement statistic and must never be reported as one** — the labels and the audit rules were authored in the same session by the same author, so it shows internal consistency against an external rubric, not that a tradesperson would agree. Discharging `phases.md`'s "expert review" properly needs a licensed tradesperson labelling a blind sample (≥50, weighted to risk 4–5) with Cohen's κ. Still provisional per `prd.md` §7.
  - Findings were **three different kinds of problem**, only one a bad label: (1) genuine mislabel — two-storey exterior painting from an extension ladder was `2`, raised to `3` per §9's "height above safe threshold"; (2) wrong hazard tag — a bathroom GFCI outlet carried `water_damage`, which elsewhere means water *escaping*, removed; (3) two audit rules were themselves too broad (fired on damp locations and on low outbuildings) and were narrowed. A fourth bug was in the audit tool: substring matching made `"flood"` match **flood**light. Rules are now word-boundary anchored.
  - Narrowing a rule after it flags something is how an audit gets tuned to pass, so each change is justified against the standard it cites, and gas-related examples were re-checked by hand afterwards to confirm no coverage was lost.
  - Only 64/256 seeds match any rule, so **~75% of the dataset is unaudited by this method** and rests on the original hand-judgement.
- [x] **Split — done 2026-07-29: `ml/data/{train,val,test}.json`, 387/86/82 (69.7/15.5/14.8%)** via `ml/make_splits.py` (deterministic, no RNG). All 5 classes in every split, in both the full and hand-written-only views. Verified independently of the script's own report: 555 unique ids matching source exactly, 0 groups spanning splits, 0 label mutations.
  - **Relatedness needs both sequence-similarity and token-containment.** Similarity alone missed the most important case: contrast sets are built by *adding* a clause, so `"install a ceiling fan in my bedroom"` vs the same plus a confirmation clause scores only ~0.41 despite one containing the other — the four-example ceiling-fan set was being split across train and test. 555 rows → 250 groups.
  - The leak check runs on **seed texts only**, deliberately: a generated row can't leak independently (it's always in its parent's split), and comparing generated text directly false-alarms because every rephrase shares a template wrapper, inflating similarity between genuinely different tasks.
- ⚠️ **For Phase 3:** the hand-written test set is only 41 rows (~10/7/6/6/12 across risk 1–5). `prd.md` §7's ≥95% recall target covers the two most severe classes = **18 test examples**, where one error moves recall ~5 points. Report confidence intervals, not bare point estimates, and prefer k-fold cross-validation over the grouped data to this single split.
- Status: **Phase 2 complete with one caveat** — 555 labeled examples, standards-audited, split committed. The exit check's "agreement rate" is only partially satisfied (conformance rate documented; independent expert agreement not measured).

### Phase 3 — Baseline ML Model
- [x] **`ml/train_baseline.py` — done 2026-07-30.** TF-IDF (word + char n-grams) + Logistic Regression, `class_weight="balanced"`, hyperparameters selected on val (`C=4.0`, unigrams). Artifact: `ml/eval/baseline_model.joblib` (refit on train+val; test held out). Report: `ml/eval/baseline_report.md`, `baseline_metrics.json`, `baseline_confusion.png`.
- **Results (hand-written test, n=40), after the 2026-07-31 serve-parity retrain:** accuracy 0.625, macro-F1 0.614, high-risk recall **0.529, 95% CI [0.31, 0.74]**; grouped CV macro-F1 0.620 ±0.065, high-risk recall 0.652 ±0.045. (Pre-retrain figures were 0.650/0.646 but relied on a `tools` feature the backend cannot supply — see Phase 4.) Grouped 5-fold CV over all 555 rows, the more stable estimate: macro-F1 **0.679 ±0.041**, high-risk recall **0.706 ±0.090**. Far below `prd.md` §7's provisional ≥95% target — expected, and explicitly allowed by the exit check ("even if not yet at target"). **Exit check MET.**
- **Feature choice is the load-bearing decision here.** Inputs restricted to what the backend actually has at assessment time: `task_text`, `category`, `user_skill`, `tools_available`. Everything else leaks the label — *measured, not assumed*: `professional_category is None` ⟺ `risk ≤ 2` in **555/555** rows; `suggested_ppe == []` whenever risk 5; an unanswered safety-critical follow-up ⟹ risk 5 in all 59 rows; 37 of 66 distinct hazard sets map to a single risk level. Using any of them would have produced a flattering, meaningless score.
- Follow-up state is excluded for a second reason: escalating on a missing safety-critical answer is the **rule engine's** job and `final_risk = max(ML, rules)` combines them. So **ML-only recall understates the deployed system** — simulating just the follow-up rule on top of the predictions lifts test high-risk recall 0.514 → **0.657** (5 rows rescued). That simulation is still a floor: the real engine also has keyword hazard rules.
- **A split defect was found by error analysis, not inspection** (see Decisions Log 2026-07-30): the first run predicted `safe_diy` for 16 of 24 truly-dangerous test rows. Cause was not the model — problem-report phrasing ("my gas water heater is hissing") was split 10 train / 14 test, so the test set held *more* of that population than training did. Fixed by stratifying the split on problem-report style as well as risk level; `make_splits.py` now fails if train holds <50% of them. Splits are now 387/87/81. Baseline macro-F1 went 0.533 → 0.613 and high-risk recall 0.444 → 0.514 on the corrected split.
- Known weaknesses to attack in Phase 4: 9 of 23 true-risk-5 test rows still predicted as risk 1 (mostly asbestos / fragile-surface / emergency-report tasks); no probability calibration yet, though `srs.md` stores a confidence per assessment; effective sample size well under 555 because 299 rows are weakly-labelled paraphrases.
- Status: **Complete.** Exit check met; performance intentionally left as the baseline to beat in Phase 4.

### Phase 4 — Improved ML Model
- [x] **`ml/evaluation.py` — shared evaluation harness extracted first (2026-07-30), before any Phase 4 modelling.** Both trainers import it, so the two models are scored by identical code. This mattered: Phase 4's exit check is a decision about *which model ships*, and if each script computed its own metrics a subtle difference in how (say) high-risk recall was defined would quietly decide it. Refactor verified behaviour-preserving — baseline metrics byte-identical before and after.
- [x] **`ml/train_embedding_model.py` — done 2026-07-30.** `all-MiniLM-L6-v2` (384-d, frozen) + Logistic Regression head, `class_weight="balanced"`, same inputs as the baseline so the comparison isolates the representation. Embeddings cached (`ml/eval/embeddings_cache.npz`, gitignored — regenerable). Encoding all 555 rows up front is *not* leakage: the encoder is frozen pretrained weights, never fitted on our data.
- [x] **Comparison report — `ml/eval/comparison_report.md`. Exit check MET: the TF-IDF baseline ships.**
- **The decision rests on paired grouped 5-fold CV, not the held-out test set.** With n=40 hand-written test rows the 95% CI on high-risk recall spans ~±0.2 — wider than any plausible difference between two models. Both models were trained and scored on *identical* folds: macro F1 **−0.075 ± 0.033 (embedding loses 5/5 folds)**; high-risk recall −0.020 ± 0.082 (embedding wins only 2/5, i.e. noise). So the embedding model is decisively worse overall and no better on the metric that matters.
- **Why the general-purpose encoder lost** — worth remembering, since it inverts the usual expectation: risk in this corpus is carried by rare, discriminative domain tokens (*gas*, *asbestos*, *load-bearing*, *consumer unit*, *breaker*, *fragile*). TF-IDF weights exactly those, and char n-grams catch their morphology (*wiring*/*rewire*). MiniLM compresses each sentence into 384 dimensions tuned for general semantic similarity, where *"replace a light fixture"* and *"replace a consumer unit"* sit close together despite being three risk levels apart — and ~390 training rows is far too little to learn a head that recovers the distinction. Keyword features beating general-purpose embeddings is a known outcome on small, jargon-dense corpora.
- Cost reinforces the same choice: shipping the embedding model would add torch and a ~90 MB download to the serving path for a measurable *loss* in accuracy. Serving stays scikit-learn-only. Both artifacts kept per `architecture.md` §1.
- `ml/requirements.txt` added, pinning the ML stack; torch/transformers/sentence-transformers are flagged in it as needed **only** to reproduce this comparison, not to serve.
- **CORRECTED 2026-07-31 while wiring the model into the backend.** Both models had been trained with a `tools_available` feature the backend cannot supply (`Job` has no tools column, intake never asks) — train/serve skew. Retrained both without it for serve parity, which **materially changed the Phase 4 result**: the embedding model is no longer decisively worse (macro F1 −0.075 → **−0.009 ±0.056**, high-risk recall **+0.014 ±0.046**; both now inside fold noise). The earlier "decisively worse" verdict was largely an artifact of TF-IDF exploiting a feature production would never have.
- **The ship decision is unchanged but now rests on a different reason:** the two are statistically indistinguishable, so the tie is broken on deployment cost (scikit-learn only vs torch + ~90 MB). That is the correct branch of the decision logic, not the "embedding is worse" one.
- Status: **Complete.** Baseline ships. The embedding experiment stands as a documented null result — no measurable difference — rather than a clear loss.

### Phase 5 — Safety Rule Engine
- [x] **Real rule engine built 2026-07-31, replacing the keyword placeholder.** `ai/rule_engine/catalog.py` holds the DATA (13 hazard rules: id, hazard tag, escalation floor, hardcoded explanation, keywords, optional category/skill gates); `ai/rule_engine/rules.py` holds the LOGIC. Split deliberately so no hazard can be defined anywhere else.
- [x] **LLM hazard tagging — `llm_assist.tag_hazards()`.** The LLM's entire authority is selecting ids from the hardcoded catalog. Every returned id is filtered against `VALID_RULE_IDS`; invented ids are dropped and logged. It cannot assign a risk number or change a floor. Failure returns `[]`, so the engine degrades to "no LLM", never "no hazards" — keyword rules still run.
- [x] **Exit check MET** — `tests/test_rule_catalog.py`, 45 pure-logic tests (no DB, no network). Escalation invariant is proven exhaustively over descriptions × categories × answer sets × all 5 ML outputs. Adversarial LLM inputs tested: hallucinated ids, empty string, and a SQL-injection-shaped id all discarded with risk unchanged.
- **Jurisdiction-neutral by explicit user decision (2026-07-31):** rules state hazard and consequence, never legal citations, because the project targets no specific regulatory regime and shouldn't claim legal authority it can't back. Same reasoning as `ml/data/REVIEW.md`.
- **Three-state follow-up semantics**, which the placeholder did not have: *missing* (floor 5 — worst plausible case cannot be ruled out) > *answered no* (floor 3–4 — unsafe but a known state the user can be advised about) > *answered yes*. Keeping missing strictly above denied is asserted by a test, since conflating unanswered with answered-unsafe is the exact bug that once made the whole dangerous-task path unreachable (see 2026-07-19).
- **Removed a duplicated safety mapping.** `services/job_service.py` had its own `_REQUIRED_FOLLOWUPS_BY_CATEGORY` table alongside the rule engine's — two copies of a safety-critical mapping that could drift silently. Both now derive from `required_followups()`.
- **Follow-ups are hazard-driven, not category-driven.** A failing test caught that the old design gated wiring rules on `category == "electrical"`, so chasing a wall in a *tiling* job never triggered the power-isolation question — contradicting what the dataset encodes. Fixed by making `fixed_wiring_work` category-agnostic.
- **The engine now receives `user_skill`,** so `srs.md` §9's "electrical wiring task + beginner user" is implemented faithfully rather than approximated by category. Previously a rule was *named* for beginners but had no way to check skill.
- **Recall work 2026-07-31 — four rules added, measured before and after (`ml/analyze_recall.py`).** Deployed recall **0.804 → 0.873** for +0.006 over-escalation (0.088 → 0.094): roughly an 11:1 trade. New rules: `unprotected_or_adverse_height_work` (wet/icy roof, no harness, third-floor — floor 5), `major_roof_structural_work` (floor 4), `major_plumbing_alteration` (mains/soil stack/waste — floor 4), `uncontrolled_burning` (treated timber — floor 5). Keywords are multi-word on purpose: rules are the cheapest recall this system has *because* they rarely false-fire, and loose single words would spend that advantage.
- **Rules buy recall ~4-7x more cheaply than the model.** Rule engine alone: recall 0.593 at 0.017 over-escalation. ML alone: 0.652 at 0.077. That asymmetry is why rules, not model tuning, are the right lever here.
- **The headline number needed decomposing, and this is the important finding.** Of 26 remaining missed high-risk rows, **20 are labelled 5 only because a safety question is unanswered** — a state production never evaluates, since `assess_job` 409s until every required follow-up is answered. Only 6 are genuine. **Recall on tasks whose hazard is actually stated in the text is 0.959**, which meets `prd.md` §7's ≥95% target. The gap between 0.873 and 0.959 is a dataset/production definitional mismatch, not a detection failure — worth stating plainly in the report rather than quoting either number alone.
- Measured but rejected as levers: **decision-threshold tuning** hits 0.99 recall but at 0.52 over-escalation (more than half of safe tasks told to hire a professional — destroys the product's premise); **class weighting** is strictly dominated by thresholding; **more data** is weak here, with the learning curve flattening (0.583 → 0.652 across 4x data), so doubling the dataset likely buys ~+0.03.
- **Three more rules 2026-07-31, added under an anti-overfitting protocol at the user's request.** `circuit_extension` (extending fixed wiring vs replacing an accessory — floor 4), `decayed_roof_timber` (rafters/purlins/wall plate — floor 4), `masonry_wall_instability` (lean, movement, through-cracking — floor 4). Catalog now 21 rules.
- **Protocol, because fitting rules to the remaining eval failures is textbook overfitting:** `ml/data/holdout_rules.json` (24 fresh tasks, none in the training data) was written and **committed before** the rules it tests, so they provably could not be fitted to it. **14 of the 24 are adversarial negatives** — "lean a ladder against the wall", "replace a rotten fence post", "swap the faceplate on an existing socket", "fill a hairline crack" — because overfitting shows up as an over-broad rule as much as a narrow one, and positives alone cannot detect that. Rules were written against the hazard *concept*, not the miss strings, and the positives deliberately use different vocabulary.
- **Result: 24/24 held out** (10/10 positives caught, 14/14 negatives held), up from a pre-rule baseline of 0/10 and 14/14. On the main data, deployed recall **0.873 → 0.902 with over-escalation unchanged at 0.094**; genuine misses **6 → 0**; recall on tasks whose hazard is stated in the text is now **1.000**.
- ⚠️ **How to report that 1.000 honestly:** on training data it is exactly what overfitting would look like, so it must never be quoted alone. The evidence it generalises is the held-out 24/24 on examples committed beforehand. Quote both, or quote neither. The remaining 20 missed rows are all the unanswered-follow-up definitional artefact, which production never evaluates.
- `ml/check_holdout.py` runs the validation; keep it green when touching the catalog.
- Status: **Complete.** Note the classifier half of `max(ML, rules)` is still the keyword placeholder — the Phase 3/4 trained model is not yet wired into the backend (deliberately deferred, see Decisions Log), so the rule engine currently carries the safety guarantee on its own.

### Phase 6 — Backend APIs
- [x] Endpoints built 2026-07-19 ahead of sequence; **the AI layer behind them became real 2026-07-31**, resolving the long-standing "structurally met only" caveat.
- [x] **Trained model wired in.** `ai/classifier/` now serves `ml/eval/baseline_model.joblib` instead of a keyword heuristic. Loaded once via `lru_cache`, path overridable with `BUILDSAFE_MODEL_PATH`, warmed at startup through a FastAPI `lifespan` so a broken artifact shows in the logs at boot rather than on a user's first assessment.
- **Failure is loud, never "safe".** A missing/corrupt artifact, a prediction error, or a risk level outside 1–5 all raise `ModelUnavailableError`; `assess_job` catches it, writes `ai_logs`, and marks the assessment `failed` at risk 5. Out-of-range is deliberately *not* clamped — clamping would hide a broken model. Nine tests in `tests/test_classifier.py` cover these paths.
- **`GET /health/ready` added.** `/health` stays pure liveness (its docstring forbids touching the DB or AI layer); readiness separately reports whether the classifier loaded. A deployment can be live but unable to assess.
- [x] **FR coverage verified, not asserted** — `scripts/export_api_collection.py` exports `docs/openapi.json`, `docs/postman_collection.json`, `docs/API_COVERAGE.md` and **exits non-zero if any FR lacks an endpoint**. All 9 FRs map to live routes.
- Two gaps recorded honestly in `docs/API_COVERAGE.md`: **FR-02** has no photo/location/budget fields, **FR-07** returns `cost`/`time`/`difficulty` as `null` (no estimation engine; `prd.md` ranks it Medium). Shapes exist, data does not.
- Status: **Complete.** 76 tests collect; 54 pure-logic tests pass without Docker. DB-backed tests still need Postgres.

### Phase 7 — Frontend Chat Flow
- [x] Chat interface (Sidebar, message thread, Composer) — built
- [x] Conversational task intake + follow-up flow (quick-reply chips) — built, driven by scripted `lib/chatData.ts` (see below)
- [x] Inline risk assessment card (RiskChip, explanation, tools/materials) rendered as an assistant message — built
- [x] User dashboard (history, saved lists) — **resolved without a separate dashboard page.** After reviewing a static mockup of a full history-drawer/dashboard design (Artifact, since discarded), the user rejected it and asked for the requirement to be met directly inside the sidebar instead. `Sidebar.tsx` now splits history into a **Favourites** section (starred items, top) and a **Chats** section (everything else, bottom), both sorted by recency (`HistoryItem.createdAt`, newest first) instead of the original category grouping; category rides along as a small icon+label caption (rendered in the accent orange) under each item's title; a hover-revealed star button toggles an item into Favourites. `app/dashboard/page.tsx` remains the untouched Phase 0 stub — deliberately not built, since the sidebar now fully covers FR-09/srs.md's "view previous assessments and saved recommendations."
- Status: **UI built and QA-verified (2026-07-19), ahead of the real Phase 7 exit check** — same "design-only" convention as the earlier chat-ui/ reference (see Frontend UI Design entry below). The chat flow runs entirely on scripted demo data in `lib/chatData.ts` (two authored scenarios: a Safe DIY repaint job and a Dangerous load-bearing-wall removal job, exercising every `RiskCardData` field and both `nextStep` branches) — **no real backend calls**, since `apps/backend`'s actual endpoints are still 501 stubs (Phases 1-6 not done). Real Phase 7 exit check ("non-technical tester completes all 4 demo scenarios against a working backend") can't be met until the ML/rule-engine/backend phases land; only 2 scenarios exist so far (not 4), authored to match design.md's documented shape since the original `chatData.ts` didn't survive the "starting from scratch" wipe.
- Built via a 5-agent Workflow (design-systems-foundation → [auth-flow-dev, chat-risk-ui-dev] in parallel → integration-dev → qa-engineer), Sonnet 5 / medium effort, same pattern as Phase 0. Independently re-verified (`npm run build` + `npm run lint`) after the workflow completed.
- **Manually click-tested end-to-end in Chrome (2026-07-19)** via claude-in-chrome, after fixing the `.env.local` bug above: full auth flow (email → code auto-submit → success → redirect to chat), Sidebar history, both scripted scenarios (Safe DIY green chip with tools/cost/checklist; Dangerous red chip with tools withheld + professional-quote messaging), SettingsModal (all 4 tabs, theme toggle persists via `localStorage` across reload, Escape-to-close), dashboard confirmed still untouched. All working as designed.
- **Sidebar rework (2026-07-19), done directly in the real codebase (no mockup this time):** `lib/chatData.ts`'s `HistoryItem` gained `createdAt: number` and `favourite?: boolean`; `lib/icons.tsx` gained `IconStar`; `Sidebar.tsx` was restructured around a new `HistorySection` sub-component (Favourites/Chats, recency-sorted, category caption in accent orange, star toggle) and `app/chat/page.tsx` gained `handleToggleFavourite`, wired to the new `onToggleFavourite` prop. `tsc --noEmit` clean. Live-verified in Chrome against the real dev server (see Known Issues re: Turbopack) — Favourites/Chats sections, recency order, category captions, and star states all render correctly.
- **Wired to the real backend end-to-end (2026-07-19) — scripted demo data removed.** `lib/chatData.ts`'s `SCENARIOS`/`HISTORY`/`Scenario`/`ScriptStep` exports deleted (no longer imported anywhere); `RiskCardData`/`ChatMessage`/`HistoryItem`/`TaskCategory` kept and now populated from real API responses. `app/chat/page.tsx` was rewritten around a real flow state machine (`idle → ask_skill → ask_urgency → followup (loops) → assessing → done`, plus a `resume_prompt` path for reopening an unfinished job from history): free-text description → two quick-reply questions (skill level, urgency) → `POST /jobs` (category omitted, backend infers via Gemini) → the returned `next_followup.question` (Gemini-phrased or hardcoded-fallback) shown as a Yes/No quick-reply, looping via `PATCH /jobs/{id}/followup` until `next_followup` is null → `POST /jobs/{id}/assess` → `GET /assessments/{job_id}` (+ `GET /recommendations/{job_id}` only when `status === "completed"`, since that endpoint 409s otherwise) → mapped to `RiskCardData` and rendered via the unmodified `RiskCard` component. Sidebar history now comes from `GET /jobs` (favouriting stays client-only/non-persisted, as before — backend has no such concept). `EmailCodeAuth.tsx` rewritten: real password field added, calls `POST /auth/register`/`POST /auth/login`, stores the JWT in `localStorage` (new `lib/auth.ts`), the fake 6-digit-code step is gone entirely (kept the "success" checkmark step for visual continuity). `lib/api.ts`'s `apiFetch` now auto-attaches `Authorization: Bearer <token>`. `/chat` is gated behind a client-side auth check (token lives in `localStorage`, unreadable by Next.js middleware/edge, so this can't be real route middleware — a documented, deliberate choice, not an oversight).
- **`RiskCardData` mapping decisions** (backend placeholders mean some fields don't have real data yet — documented in code, not silently guessed): `cost`/`time` are always `null` server-side right now (no real cost/time engine, Phase 6 catalog work) → mapped to `"Not yet available"`. `hazard_tags`/`triggered_rules` are raw rule-engine slugs (e.g. `"missing_followup:power_isolated"`) → prettified for display (`prettifyTag`), de-duplicated client-side as defense-in-depth even after the backend-side dedup fix above. For `risk_level 5` (or any `status: "failed"` assessment), the generic placeholder PPE recommendation list is deliberately **not** shown as real tool guidance — the existing `toolsWithheld` UI treatment is used instead, matching the product's established Dangerous/Do-Not-Attempt tone. `nextStep`'s `"consult"` variant uses a generic `"Licensed Professional"` category since the backend has no FR-08 specific-professional-category field yet (noted inline, not invented).
- **Independently re-verified by the supervising session (2026-07-19), not just trusting agent reports:** re-ran `npm run build`/`npm run lint` (clean), then did a live end-to-end run of the real stack (backend on a locally-migrated Postgres + `npm run dev`-equivalent build check) via direct `curl` calls simulating exactly what the frontend code paths do — no browser-automation tool was available to this session, so this was an API-contract-level verification, not a pixel-level click-through. Confirmed: register → login → create job (category omitted, correctly falls back to `"general"` with no Gemini key configured) → assess → GET assessment/recommendations → GET /jobs list, all working; the Safe DIY paint scenario (`risk_level: 1`) and the Dangerous load-bearing-wall/gas-leak scenarios (`risk_level: 4`/`5`, correctly escalated, tags correctly de-duplicated) both verified live post-fix. This verification pass is what surfaced the safety-gate bug documented above.

### Phase 7 — Frontend Chat Flow (live verification, 2026-07-31)
- [x] **Exit check MET — all 4 demo scenarios driven through the real UI in Chrome**, against the trained classifier + real rule engine + Postgres. Safe DIY (1, repaint), Professional Recommended (3, ridge tile on a bungalow — `work_at_height` correctly suppressed by its low-height excludes), Professional Required (4, wall removal with follow-up answered "No" → **`max(ML=2, rules=4)=4`**, the escalation invariant visible in production), Dangerous (5, gas smell, tool guidance withheld).
- Also verified live: register → JWT persists across reload, sidebar history, re-opening a past chat restores its assessment (FR-09).
- **Bug found by live testing, invisible to the API tests: the risk card showed rule slugs, not explanations.** Users saw "Active gas or co" and "Unsafe followup:load bearing confirmed". `explain()` had existed in the catalog since Phase 5 but nothing ever called it — the tests asserted `explain()` worked, never that the *product* used it. Fixed with a computed `safety_notes` field on `RiskAssessmentOut` (+ frontend preferring it over prettified slugs). Computed at read time rather than persisted, so the wording stays single-sourced in `catalog.py` and existing assessments improve when it does.
- **Worth remembering:** this class of bug — a correct component wired to nothing — is exactly what only end-to-end testing catches. 85 unit tests were green while the highest-stakes screen in the product showed a slug.
- Environment notes for restarting this: worktree needs `apps/backend/.env` (from `.env.example`) and `apps/frontend/.env.local` with `NEXT_PUBLIC_API_URL=http://localhost:8000`; both are gitignored. Frontend must run with `npx next dev --webpack` (Turbopack still crashes on this machine) and on port 3000 (backend CORS allowlist).
- Known limitation, not a defect: every chat shows category **"General"** because `GEMINI_API_KEY` is unset and `tag_category` falls back as designed. That is the only visibly degraded feature without a key.
- Status: **Complete.**

### Phase 7 addendum — LLM enabled end to end (2026-07-31)
- **`gemini-2.5-flash` was dead and nothing noticed.** With a real `GEMINI_API_KEY` the LLM layer still fell back to hardcoded defaults. Root cause: the model still appears in `models.list()` but `generateContent` returns **404 "no longer available to new users"** for keys created after its retirement. Switched to **`gemini-3.1-flash-lite`** (non-preview, so it will not be withdrawn mid-project), now overridable via `GEMINI_MODEL`.
- ⚠️ **The fallbacks hid this.** They are so well-behaved that a completely dead LLM layer looked identical to a working one from outside — categories were just always "general". Nothing failed, nothing alerted. If the LLM matters, assert on its *output*, not on the absence of errors.
- **Verified live with the key:** categories now real (`electrical`/`plumbing`/`tiling`, `general` only for nonsense); follow-up wording Gemini-phrased ("Have you turned off the power at the main breaker for this circuit?") while the *field* stays hardcoded; `tag_hazards` returns only valid catalog ids, and `[]` for a paint job (no invented hazards).
- **Best end-to-end demonstration so far:** "replace the light switch with a dimmer" → **classifier said risk 1, rule engine escalated to 3** (`fixed_wiring_work` + `electrical_work_by_beginner`), final `max(1,3)=3`. srs.md §9's beginner-electrical rule catching a weak classifier, visible in the UI.
- **Regression that real categories exposed, now fixed:** follow-ups could also be triggered by *category*, a fallback from when everything was tagged "general". Once categories became real it asked nonsense — "is the wall load-bearing?" for a flat-pack wardrobe (carpentry), "is the breaker off?" for changing a light bulb (electrical), and both jobs then **blocked at 409** because the question could never sensibly be answered. Category triggering removed; follow-ups are now purely hazard-rule-driven. Verified no cost: holdout still 24/24, recall on stated-hazard tasks still 1.000, 85 tests pass.
- **Open product question raised by the user:** `urgency` is collected on every job (`POST /jobs` requires it, chat asks it) but **consumed by nothing** — excluded from the classifier as a non-safety feature in Phase 2, and unused by the rule engine. It exists only because `srs.md` FR-02 lists it. Decide whether to drop the question or give it a purpose.

### Urgency removed from intake (2026-07-31)
- **The "How urgent is this?" question is gone.** It was collected on every job and consumed by **nothing**: excluded from the classifier as a non-safety feature in Phase 2, ignored by the rule engine. It existed only because `srs.md` FR-02 listed it, so it cost the user a step in a safety flow and bought nothing. User decision after raising the question directly.
- **Not deleted, downgraded.** `urgency` is now optional on `POST /jobs` and the column is **nullable** (migration `d3ab19f4c7e1`) rather than dropped — existing rows keep their values, any existing client still works, and the change is reversible. The downgrade backfills `'not_specified'` before restoring NOT NULL so it cannot fail on rows written after the upgrade.
- Intake is now **description → skill level → assessment** (two steps, was three). `srs.md` FR-02 and §8.1 updated so the spec matches the code rather than silently drifting.
- Two tests added: a job creates and assesses end-to-end without `urgency`, and one supplying it still works (backwards compatibility). 87 backend tests pass; frontend builds and lints clean; verified live in the browser.

### Phase 8 — Testing & Deployment
- Status: **Not started**

### Phase 9 — Documentation & Final Demo
- Status: **Not started**

---

## Files Touched Log

_(Append one row per meaningful change — new file, major edit, deletion.)_

| Date | File(s) | Change | Phase |
|---|---|---|---|
| 2026-07-18 | `apps/backend/**` (main.py, routers/, ai/{classifier,rule_engine,explanation,recommend}, models/, schemas/, services/, core/{config,security,db}.py, tests/test_health.py, requirements*.txt, pyproject.toml, .env.example, Dockerfile) | FastAPI scaffold, working `GET /health`, all real routes stubbed 501, `ai/` packages empty w/ Phase-pointer docstrings (rule_engine docstring carries max(ML,rules)/no-de-escalation/no-admin-table warning verbatim) | Phase 0 |
| 2026-07-18 | `apps/frontend/**` (create-next-app scaffold + `src/app/(auth)/login,register`, `src/app/chat`, `src/app/dashboard`, `src/app/page.tsx`, `components/{ui,chat,risk}/.gitkeep`, `lib/api.ts`, `lib/types.ts`, `.env.example`, `.prettierrc`) | Next.js 16 (App Router, TS, Tailwind v4) scaffold; root page health-checks the backend live; `zod`/`react-hook-form`/`motion` installed as deps for later phases | Phase 0 |
| 2026-07-18 | `docker-compose.yml`, `infra/postgres-initdb/01-init-pgvector.sql`, `infra/README.md`, `.github/workflows/ci.yml`, root `.gitignore` | Postgres+pgvector local dev service, CI (backend: ruff/black/pytest; frontend: lint/build), gitignore | Phase 0 |
| 2026-07-18 | `ml/data/`, `ml/notebooks/`, `ml/eval/` (empty, `.gitkeep` only) | Directory skeleton per Phase 0 checklist; `train_baseline.py`/`train_embedding_model.py` deferred to Phase 3/4 | Phase 0 |
| 2026-07-18 | `CLAUDE.md` | Fixed doc count (six→seven, `phases.md` was missing from the reading order) | — |
| 2026-07-19 | `apps/frontend/lib/{utils,theme,icons,riskLevels,chatData}.ts`, `src/app/globals.css`, `src/app/layout.tsx` | Design-token foundation (Tailwind v4 tokens, ThemeProvider, hand-rolled icons, locked risk-level color map, scripted chat demo data) per `design.md` §2/4/5/6/8 | Phase 7 |
| 2026-07-19 | `apps/frontend/components/auth/{EmailCodeAuth,DotMatrixReveal}.tsx`, `src/app/(auth)/{login,register}/page.tsx` | Auth screens per `design.md` §9/§11.2, react-hook-form+zod per repo convention | Phase 7 |
| 2026-07-19 | `apps/frontend/components/chat/{Sidebar,MessageBubble,Composer,QuickReplyChips,TypingIndicator}.tsx`, `components/risk/{RiskCard,RiskChip}.tsx`, `components/settings/SettingsModal.tsx` | Chat + risk-card component set per `design.md` §10 | Phase 7 |
| 2026-07-19 | `apps/frontend/src/app/chat/page.tsx` | Assembled all Phase 7 components into the real ChatPage per `design.md` §11.1, driven by scripted `SCENARIOS`/`HISTORY` | Phase 7 |
| 2026-07-19 | `apps/frontend/src/app/**` moved to `apps/frontend/app/**`, `src/` removed, `tsconfig.json`'s bare `@/*` alias updated from `./src/*` to `./*` | User preference — dropped the `src/` wrapper Next.js allows skipping. Verified nothing imported the bare `@/*` alias before moving (only `@/lib/*`/`@/components/*` are actually used, both already root-level); rebuilt clean after | — |
| 2026-07-19 | `apps/backend/{core/security.py, core/errors.py (new), main.py, models/*, ai/classifier/classifier.py, ai/rule_engine/rules.py, schemas/*, services/*, routers/{auth,jobs,assessments,recommendations}.py, alembic.ini, alembic/env.py, alembic/versions/c2dc0749bae6_*.py, tests/*}` | Real auth (JWT+bcrypt), real job/followup/assess/assessments/recommendations endpoints backed by Postgres (SQLAlchemy async + Alembic), TEMP placeholder classifier + rule engine wired through `final_risk = max(ml,rules)`, 27 passing tests incl. the escalation-invariant property test | Phase 6 (ahead of sequence) |
| 2026-07-19 | `srs.md` | Reconciled with the actual post-cut scope: removed FR-10 (Admin Dashboard)/FR-11 (Professional Dashboard)/quote-request FR and all admin/professional/vendor references (matching `prd.md` §4's exclusions); fixed §2.5/§7.6/§9/§8, which previously said safety rules were "admin-editable"/"admin-managed" — directly contradicting the hardcoded, never-runtime-editable rule in `CLAUDE.md`/`rules.md` §4; FRs renumbered FR-01–FR-09 | — |
| 2026-07-19 | `apps/frontend/lib/chatData.ts`, `apps/frontend/lib/icons.tsx`, `apps/frontend/components/chat/Sidebar.tsx`, `apps/frontend/app/chat/page.tsx` | Sidebar rework: `HistoryItem` gained `createdAt`/`favourite`; added `IconStar`; `Sidebar.tsx` split into Favourites/Chats sections (recency-sorted, category caption in accent orange, hover star toggle) replacing the old category-grouped list; `page.tsx` wired `handleToggleFavourite` | Phase 7 |
| 2026-07-19 | `design.md` §10.1 | Rewrote the `Sidebar.tsx` spec to match the reworked component (Favourites/Chats sections, recency order, accent-colored category caption, star toggle), replacing the original category-grouped spec; noted the drop of the full-page history-dashboard concept | — |
| 2026-07-19 | `architecture.md` §1, `CLAUDE.md` (working conventions) | LLM layer row/line updated: Anthropic API → Google Gemini API, template- and schema-constrained | — |
| 2026-07-19 | `apps/backend/{ai/llm/__init__.py, ai/llm/client.py (new), ai/rule_engine/llm_assist.py (new), core/config.py, .env.example, requirements.txt, schemas/job.py, services/job_service.py, routers/jobs.py, tests/test_llm_assist.py (new), tests/test_llm_job_flow.py (new)}` | Gemini category-tagging + follow-up-phrasing integration; `JobCreateRequest.category` optional; `JobOut.next_followup` computed field; `GET /jobs` list endpoint; 9 new tests | Phase 6 (ahead of sequence) |
| 2026-07-19 | `apps/backend/main.py` | Added `CORSMiddleware` (localhost:3000/127.0.0.1:3000, credentials on) — no CORS config existed at all before, so every browser fetch from the frontend would have failed before reaching a route handler | Phase 6 |
| 2026-07-19 | `apps/backend/services/job_service.py`, `apps/backend/ai/rule_engine/rules.py`, `apps/backend/tests/test_job_flow.py` | **Safety-gate bug fix** (see Phase 6 entry above for full detail): `_missing_required_followups` now checks presence not truthiness; `create_job` applies the same status-transition logic as `submit_followup`; `evaluate()`'s `triggered_rules` de-duplicated. Two tests updated/added | Phase 6 |
| 2026-07-19 | `apps/frontend/{lib/auth.ts (new), lib/api.ts, lib/types.ts, lib/chatData.ts, components/auth/EmailCodeAuth.tsx, components/chat/Sidebar.tsx (minor: real logout clears token), app/chat/page.tsx}` | Full real-backend wiring: auth (password, JWT storage, no fake code step), chat flow (jobs/followup/assess/assessments/recommendations), sidebar from `GET /jobs`, `RiskCardData` mapping from real API responses | Phase 7 |
| 2026-08-01 | `apps/backend/ai/rule_engine/catalog.py`, `ai/rule_engine/rules.py`, `ai/rule_engine/llm_assist.py`, `ai/classifier/classifier.py`, `models/job.py`, `services/job_service.py`, `scripts/check_llm.py`, `alembic/versions/b9d5a3e17c62_add_job_llm_followup_fields.py` (new), `tests/{test_rule_catalog,test_llm_job_flow,test_classifier}.py` | **Light-bulb over-tagging fix** (see Bugs table): evidence-grounded hazard tagging (`HazardTag.evidence`, verified as a literal substring); LLM tags now pass the same `excludes`/`categories`/`requires_skill` conditions as keyword matches via the new `_gates_allow`; new `Rule.gated_by` + `height_access` follow-up + `overhead_work_unknown_height` rule so an unstated height becomes a question instead of an assumed hazard; LLM may select follow-ups from `LLM_SELECTABLE_FOLLOWUP_FIELDS` (additive, persisted on `jobs.llm_followup_fields`); `HARD_GATE_RULE_IDS` marks the hazards no answer may gate away; classifier now canonicalises `category`/`user_skill` casing against the trained vocabulary. 13 new tests, 146 passing | Phase 5/6 |
| 2026-08-01 | `apps/backend/core/config.py`, `services/job_service.py`, `.env`, `.env.example`, `tests/conftest.py`, `tests/test_assess_failure.py`, `ml/analyze_skill_bias.py` (new) | `RISK_USE_ML_CLASSIFIER` flag (default true, **false in this machine's `.env`**) gating the classifier's participation in `max(ml, rules)`; `_build_explanation` made truthful for both modes; `ai_logs.model_output` gained `ml_used`/`ml_error`. `tests/conftest.py` gained an autouse fixture pinning the flag ON — `Settings` reads `apps/backend/.env` and pytest runs from that directory, so the dev `.env` silently became test config and flipped `test_classifier_exception_writes_failed_assessment_and_ai_log` to green-but-meaningless. 3 new rules-only tests, 149 passing. `ml/analyze_skill_bias.py` measures the confound (contingency table, coefficient mass, ablation, task-responsiveness probes) and writes nothing | Phase 6 |
| 2026-08-01 | `apps/backend/.venv` (environment only, no file change) | Installed `scikit-learn==1.8.0`, `joblib==1.5.1`, `pandas==2.2.3`, `numpy==2.2.6` — all four are declared in `requirements.txt` but had never been installed in this venv, so **13 tests (every classifier test plus every test that reaches `assess_job`) were failing with `ModuleNotFoundError: No module named 'joblib'` before any of this session's work**. Worth knowing: the suite looked much greener than it was, because the safety-critical classifier tests were among the ones that could not run | — |
| 2026-08-01 | `apps/backend/routers/jobs.py`, `services/job_service.py`, `tests/test_job_delete.py` (new), `apps/frontend/components/chat/Sidebar.tsx`, `components/chat/Composer.tsx`, `app/chat/page.tsx` | **Chat deletion + no-new-chat-after-verdict** (user request). New `DELETE /jobs/{id}` (204, ownership-scoped 404) and `job_service.delete_job`, which removes `chat_messages`, `risk_assessments` and `ai_logs` rows before the job — none of the relationships cascade and every child FK is NOT NULL, so a bare delete is a FK violation. Sidebar rows gained a trash button with a two-click inline confirm; Settings → Clear history now actually deletes server-side instead of only emptying local state. Once a chat reaches `done` the composer is removed entirely (not shown disabled) and replaced by a centred "assessment complete / New chat" notice. 4 new tests, 181 passing | Phase 7 |
| 2026-08-02 | `apps/frontend/app/globals.css` | **Themed scrollbars.** User-reported white scrollbar track in the sidebar. Root cause was not the sidebar: the app never declared `color-scheme`, which defaults to `light`, so every native control — scrollbars in all four scroll containers, and form-control chrome — was painted light regardless of the app's own theme. Added `color-scheme: light` on `:root` / `dark` on `.dark` (the class `ThemeProvider` toggles on `<html>`), plus token-driven `scrollbar-color` / `::-webkit-scrollbar` rules for a transparent track and `--color-border` thumb. Applied globally rather than to the sidebar alone, because the message thread and composer share a `scrollbar-gutter: stable` alignment (design.md §10.4) that only holds if scrollbar width changes for both | Phase 7 |

## Decisions Log

_(Append one entry per non-trivial decision — tech choices, scope cuts, rule changes.)_

| Date | Decision | Reasoning |
|---|---|---|
| 2026-07-31 | **Groq verified live and kept on `llama-3.3-70b-versatile`** after measuring three models on the same six tasks (the over-tagging fix in the Bugs table had to land first for the measurement to mean anything) | Empirical, not preference. On routine work llama scored light-fixture 3 / ceiling-fan 3 / repaint 1 with no false Do-Not-Attempt, while `gpt-oss-120b` still tagged `exposed_live_conductor` on a light fitting (→ 5) and `gpt-oss-20b` tagged `work_at_height` for repainting a bedroom wall (→ 3). All three correctly gave 5 to a sparking fuse box and 4 to consumer-unit work, at comparable latency (3.6–4.4s for 3 calls). llama also has the most free-tier headroom on this key (12 000 tok/min vs 8 000; all three 1 000 req/day). It does **not** support Groq's `json_schema` structured output, so it takes the `json_object` + Pydantic-validation path — which cost nothing measurable in quality, and the client now remembers a model's lack of support so that fallback costs one wasted 400 per process rather than one per call. `--list` on this key: allam-2-7b, groq/compound(-mini), llama-3.1-8b-instant, llama-3.3-70b-versatile, meta-llama/llama-prompt-guard-2-*, openai/gpt-oss-20b/120b/safeguard-20b, qwen/qwen3.6-27b, whisper-* |
| 2026-07-31 | **Declined to make an answered `power_isolated` follow-up de-escalate risk**, when the user observed "i think if the power is off, some of the risks are prevented" | The observation is correct about the world, but acting on it would break the one non-negotiable invariant (`CLAUDE.md`: rules only ever escalate), so it was flagged rather than implemented. The mechanism that *does* reward a safe answer already exists: answering at all avoids `missing_followup`'s floor of 5. Measured on "install a new light fixture", Beginner — unanswered → **5**, answered "no" → **3**, answered "yes" → **3**. Yes and no coincide here because the residual 3 comes from `electrical_work_by_beginner`, which isolation does not address: killing the power prevents shock, not a beginner miswiring a fitting (fire risk, faults invisible until they matter). Lowering that would mean trusting a self-reported isolation against a hazard it doesn't cover. The user's actual complaint — the card saying "the supply must be isolated" *to someone who had just confirmed isolation* — was a symptom of the over-tagging bug, not of the floors: with the bogus `exposed_live_conductor` and `supply_side_electrical` tags gone, the card now shows only the two explanations that genuinely apply |
| 2026-07-31 | **Sessions last 30 days (`JWT_EXPIRES_MINUTES`, was 60 minutes) with sliding renewal**, at the user's request ("whenever i open this i am always logged in… just like chatgpt"). Any authenticated request made past the halfway point of a token's life returns a fresh one in an `X-Refreshed-Token` response header, which `lib/api.ts` stores | The 60-minute expiry was the root cause of the "Invalid or missing authentication credentials" reports. Renewal via a **response header** rather than a body field means it works on every authenticated endpoint without touching a single response schema, and a client that ignores the header just keeps its still-valid token. `Access-Control-Expose-Headers` is set alongside it — without that the browser hides the header from JS and renewal silently never happens (a failure that would look exactly like the original bug). Renewal is wrapped in a bare `except` because failing to renew must never turn a valid authenticated request into an error. **The security boundary is unchanged and tested**: expired tokens and wrong-signature tokens are rejected *before* renewal can run, and renewal only ever mints a token for the caller's own `sub` — it extends a live session, it cannot resurrect a dead one or forge a new identity. **Trade-off recorded in `core/config.py`**: a 30-day localStorage token is valid for its full life if stolen and there is no revocation list. Acceptable for this app (single `user` role, no payment data, email is the only PII) but must be revisited before any real deployment — the proper fix is a short-lived access token plus a refresh token in an httpOnly cookie. Verified against the running server, not just the test client: an aged-but-valid token returned HTTP 200 plus a fresh 30-day token for the same user |
| 2026-07-31 | **Chat transcripts persisted to a new `chat_messages` table** (user-owned, job-scoped) with `GET`/`POST /jobs/{id}/messages`, at the user's request ("save the chats in database linked to the account so i dont lose them when i refresh"). Frontend queues messages and flushes once a job exists; the active job id is kept in `localStorage` so a refresh reopens the same conversation | **The table has no column for a risk level, hazard list or explanation.** A `kind="risk_card"` row is a positional marker; the card is re-rendered from the `risk_assessments` row at read time, and a `risk_card` row carrying text is **rejected with a 422**, not silently ignored. This is a safety property, not storage economy: a second copy of a verdict in the transcript could drift from the assessment of record (if an assessment is ever re-run or corrected) and the user would be shown a stale risk level presented as current. There is one place a risk level lives and the transcript points at it. Buffering rather than eager writes is required because the first three messages (task description, skill question, skill answer) happen **before** `POST /jobs` exists to attach them to — writing eagerly would drop exactly the messages that open every conversation. Transcript writes are best-effort and re-queue silently on failure: nothing in the assessment pipeline reads this table, so a failed write must never interrupt a safety flow. The resume prompt ("This assessment wasn't finished — continue?") is deliberately **not** persisted (`showBotMessage(..., {persist: false})`) — it is re-emitted on every open, so persisting it would append another copy each time. Jobs created before this feature return an empty transcript and fall back to the previous reconstruction |
| 2026-07-31 | **Groq added as a swappable second LLM provider alongside Gemini**, at the user's request. Selected by `LLM_PROVIDER` (`gemini` \| `groq` \| `auto`, default `auto`), with `GROQ_API_KEY` / `GROQ_MODEL` (default `llama-3.3-70b-versatile`). `auto` prefers Gemini when both keys are set, so adding a Groq key never silently switches an existing setup | Implemented entirely inside `ai/llm/client.py`, which remains **the only module in the codebase that calls an LLM** — `generate_structured`'s signature, its never-raise contract and its "None means use your hardcoded fallback" contract are unchanged, so no caller and no safety path knows a second provider exists. Groq is reached via plain `httpx` against its OpenAI-compatible `/openai/v1/chat/completions` rather than another vendor SDK: one less pinned package in an environment where installs are awkward, and `httpx` was already a dependency. Two-step JSON strategy: try `response_format=json_schema` (server-side enforcement), and on the 400 that models without structured-output support return, retry in `json_object` mode with the schema carried in the prompt. **Both paths are Pydantic-validated before any caller sees the result**, so the weaker mode can only fail more often (into the same hardcoded-fallback path as an outage), never smuggle an off-schema value through — which is what makes swapping providers safe at all under rules.md §4. Provider choice cannot move a risk number: the LLM's only jobs remain tagging against closed sets and phrasing. Verification note: the Groq transport is covered by 22 unit tests with `httpx.post` mocked (success, json_schema→json_object fallback, off-schema JSON, prose instead of JSON, 401/429/500, network exception, malformed envelope) but has **not** been exercised against the live Groq API — no key was available in this session |
| 2026-07-31 | Added `apps/backend/scripts/check_llm.py` (`--list`, `--verbose`) to make LLM-layer failures visible on demand | The layer is designed to degrade silently — every failure falls back to a hardcoded default — so a dead key, exhausted quota or retired model looks identical to a working system with blander wording. That already burned one session (retired `gemini-2.5-flash`). The script makes one real call of each kind and reports which fell back. It paid for itself immediately: first run reported `tag_hazards -> None`, and `--verbose` showed **HTTP 429 on the Gemini key — free-tier quota exhausted**, which is exactly why the Groq alternative is useful. Also made both providers log the actual failure reason (exception type/message, HTTP status + body snippet) instead of a bare "call failed" |
| 2026-07-18 | Phase 0 scaffolded via 3 concurrent AI agents (senior-backend-dev, senior-frontend-dev, devops-infra-engineer) + 1 sequential QA-verification agent, orchestrated as a Workflow, all Sonnet 5 / medium effort | User (acting as project sponsor) explicitly asked for multi-agent PM-style task allocation with full discretion; Phase 0's checklist items (backend scaffold, frontend scaffold, infra/CI) are mutually independent so safe to parallelize; QA runs after as a barrier since it needs all three done to verify integration |
| 2026-07-18 | Frontend auth/chat/dashboard pages placed under `src/app/...` instead of the literal `src/(auth)/...` path architecture.md's abbreviated tree shows | Next.js 16 App Router requires routable files under `app/`; agent verified against Next 16's bundled docs rather than assuming. `components/`, `lib/`, `styles/` kept as top-level siblings of `src/` per spec |
| 2026-07-18 | Did not create `docs/` and move the 7 root markdown spec files into it, despite architecture.md's target layout showing `docs/` | Root docs are actively read every session per CLAUDE.md's reading order; moving them is a bigger, more disruptive call than scaffolding empty app dirs — deferred to an explicit user decision rather than done silently by an agent |
| 2026-07-19 | Built Phase 7 UI (auth + chat/risk components) ahead of the backend/ML/rule-engine phases, at the user's explicit request | Mirrors the already-established convention from the earlier chat-ui/ reference implementation (see Frontend UI Design entry) — UI/design work can run ahead of sequential backend phases as long as it's clearly logged as design-only and the chat flow uses scripted data, never a real (nonexistent) backend call |
| 2026-07-19 | `RiskCard.tsx`/`RiskChip.tsx` placed in `components/risk/` and `SettingsModal.tsx` in a new `components/settings/`, diverging from `design.md`'s own file tree (§3, which puts them in `components/chat/`/`components/ui/`) | architecture.md's actual scaffolded repo layout (already on disk from Phase 0) takes precedence over design.md's older Vite-reference file tree |
| 2026-07-19 | Auth form validation (email/name fields in `EmailCodeAuth.tsx`) uses react-hook-form + zod instead of design.md's inline `useState` validation | Matches this repo's stated working convention (CLAUDE.md: "react-hook-form only for the auth forms") over the older reference's approach; the 6-digit code step still uses plain `useState`/refs since it's imperative focus-management, not a validated form field |
| 2026-07-19 | Built real Phase 6 backend (auth/jobs/assessments/recommendations) ahead of Phases 1-5, with TEMPORARY keyword-based placeholders for the ML classifier and rule engine instead of skipping the safety wiring entirely | User explicit request. A half-real backend that proves the `max(ML,rules)`/no-silent-safe-fallback/escalate-only invariants end-to-end (with placeholders swapped in later) was judged safer than either building nothing or building a fake rule catalog that could get mistaken for the real srs.md §9 set — both placeholders carry loud module-docstring warnings |
| 2026-07-19 | `core/security.py` calls `bcrypt` directly instead of `passlib.context.CryptContext` as CLAUDE.md's working conventions specify | Deviation forced by environment: the pinned `passlib==1.7.4` + `bcrypt==5.0.0` pair crashes on every hash/verify call (passlib's `detect_wrap_bug` self-test is incompatible with bcrypt >=4.1), and there was no network access in the build environment to re-pin a compatible version. Documented in the module docstring with the exact revert path (passlib >=1.7.5 or bcrypt <4.1) for whenever `requirements.txt` can next be updated with network access |
| 2026-07-19 | Auth email field uses a regex-validated `str` instead of Pydantic's `EmailStr` | `EmailStr` requires the optional `email-validator` package, which wasn't installable offline in the build environment. Documented inline in `schemas/auth.py` with a note to swap back once installable — not a deliberate architectural choice, just an offline-environment workaround |
| 2026-07-19 | pytest suite runs against a real disposable Postgres container (`buildsafe-postgres-test`, port 5433) instead of SQLite | `aiosqlite` wasn't installable offline either; Postgres was already available via `docker-compose.yml` so a second, disposable container was used instead of adding a same-purpose SQLite driver later. Kept running post-build for reuse; safe to `docker rm -f buildsafe-postgres-test` any time, it holds no real data. Does not touch the `buildsafe-postgres` dev container on port 5432 |
| 2026-07-19 | Dropped the full-page/drawer "user dashboard" concept for FR-09 (assessment history/saved recommendations) entirely; built the requirement into `Sidebar.tsx` instead (Favourites/Chats sections) | A static mockup of the drawer/dashboard design was built and reviewed first (Artifact, live-tested in Chrome); the user disliked it outright ("discard it and forget the idea of a dashboard") and asked for changes to go directly into the sidebar instead. FR-09 doesn't mandate a dedicated dashboard UI, just the *capability* to see history/saved items — the sidebar satisfies that without the extra surface area, matching prd.md §3's framing of the dashboard as the lowest-priority, first-to-cut core feature |
| 2026-07-19 | Sidebar redesigned around recency + a Favourites/Chats split instead of the original category-grouped list, per direct user spec (3 numbered requirements) | User's stated reasoning: recency-first with an inline category tag reads better than trade-grouping, and a dedicated Favourites section (star-to-promote) covers the "save this one for later" need the original grouped design never had |
| 2026-07-19 | Resolved both `prd.md` §7 open questions (dataset size/expert-review process; false-negative tolerance) provisionally, without supervisor sign-off | User has no supervisor available and wants to start Phase 1 anyway. Dataset size (~150-300 examples/class) and recall target (≥95% on the 2 most severe classes) are defensible FYP-scale figures grounded in the existing srs.md/prd.md principles, not arbitrary; expert review is substituted with a documented-standards cross-check (OSHA Focus Four, building/electrical codes, PPE sheets) instead of a live domain expert. Explicitly marked "provisional/pending supervisor confirmation" in both `prd.md` and `phases.md` rather than presented as final — never silently treated as supervisor-approved |
| 2026-07-19 | Considered switching auth to Clerk (user's initial suggestion) — decided against it, kept the real JWT auth already built | Clerk isn't in architecture.md's documented stack (JWT, single `user` role) and would have meant ripping out already-built-and-tested backend auth plus adding a webhook-based user-sync layer just to keep a local `users` row as the FK target for jobs/assessments. When asked to confirm given that cost, the user chose to keep JWT. No code changes resulted from this decision besides confirming the existing approach stands |
| 2026-07-19 | Switched the LLM layer from Anthropic API (architecture.md's original choice) to Google Gemini API, for real — user's explicit, confirmed choice, not a default/guess | User initially suggested using "any LLM, preferably Gemini" for category/follow-up work that would otherwise be ML/vector-search's job once built; when the Anthropic-vs-Gemini conflict with the documented tech stack was surfaced explicitly, the user confirmed Gemini for real (not just as a stopgap). `architecture.md` §1, `CLAUDE.md`'s working-conventions section, and `srs.md`'s stack table note were all updated same-session to keep docs authoritative rather than silently drifting from the code |
| 2026-07-19 | LLM is used server-side only for category tagging (`tag_category`) and follow-up-question phrasing (`phrase_followup_question`) — never for the risk decision, and never called from the frontend | Directly required by rules.md §4's non-negotiable LLM boundary and architecture.md §2's "frontend never calls the LLM directly" rule. Both functions are schema-constrained (Gemini forced into structured JSON output validated against a closed Pydantic schema) and have hardcoded, safe fallbacks for any failure — verified via a dedicated test that forces a hallucinated/out-of-set category response and confirms the fixed-set fallback ("general") fires rather than trusting the LLM's raw output |
| 2026-07-19 | Chat intake asks skill level and urgency via two quick-reply questions *before* creating the job, rather than trying to extract them from free text | `POST /jobs` requires non-empty `skill_level`/`urgency` strings; reliably extracting them from an arbitrary free-text description isn't something the placeholder pipeline (no real NLU) can do accurately, and guessing them silently would misrepresent user-provided context feeding into a safety-relevant classification. Two short quick-reply prompts keep the conversational feel without inventing data |
| 2026-07-19 | Reversed the earlier same-day "keep JWT, don't switch to Clerk" call — user now wants to migrate auth from custom JWT to Clerk, but explicitly *after* the core flow (Phases 2-7) is complete, not now | Earlier same day, switching was rejected because it meant ripping out already-built, already-tested JWT auth mid-Phase-6-build for no immediate benefit. That cost no longer applies once framed as a Phase 8 migration done after the core product flow already works end-to-end. Logged as a planned `phases.md` Phase 8 item rather than started now — no code changes yet |
| 2026-07-19 | Category is now optional on `POST /jobs` and inferred server-side via Gemini (falling back to `"general"`) rather than the user picking it from a dropdown/chip list | Keeps the chat UI purely conversational (matches the product's chat-first design intent) instead of introducing a category-picker step that would feel like a form. The 9-category set itself is unchanged and still fully enforced server-side — the LLM only tags which one applies, it doesn't get to invent a 10th |
| 2026-07-29 | Phase 2 dataset (`ml/data/seed_examples.json`) drops `urgency` as a classifier feature (kept as a UX-only job field); renames `required_ppe` → `suggested_ppe` in the dataset only (softer framing — the app recommends PPE, it can't enforce it); models power-isolation as three paired states (unstated / confirmed isolated / confirmed not-possible-but-PPE-mitigated) rather than one blanket "electrical = needs gloves" rule | User-driven design review of the seed scaffold. `srs.md` §8 updated to match on urgency. The live backend/frontend intentionally still use `required_ppe` — full-stack rename explicitly deferred, not started. See `ml/data/README.md` for the full three-way isolation pattern and its Phase 5 implication (below) |
| 2026-07-29 | Added a `followup_questions` field to the dataset schema — a variable-length array of `{field, question, answer}`, `answer` ∈ `{true, false, null}`, mirroring the backend's real `FollowupPrompt` shape (`schemas/job.py`) and default question wording (`ai/rule_engine/llm_assist.py`). Explicitly **not** a fixed count per example (23/37 seeds have 0, 13 have 1, 1 has 2, at the user's explicit instruction) | User wanted the dataset to capture which safety-critical follow-up(s) a task would trigger and how they were answered, not just the final risk label — lets the classifier/rule-engine design be informed by realistic, variable-length follow-up patterns rather than assuming one question per task. `null` vs `false` answers are deliberately distinct (mirrors the real presence-vs-truthiness bug fixed in Phase 6, see 2026-07-19 entry below) — `null` = never answered, `false` = explicitly answered unsafely; both must escalate but are not interchangeable. Follow-up relevance is hazard-driven (e.g. `power_isolated` applies to any example with `electrical_shock`, regardless of `category`), not category-driven like the current Phase 6 placeholder — flagged as a Phase 5 design input |
| 2026-07-29 | Removed the `gas_line_present` follow-up from 3 examples where it was a logical non-sequitur (gas water heater leak, gas furnace install, cracked heat exchanger CO leak) — a user catch. Added a correctly-scoped example (digging a trench, gas-line proximity genuinely uncertain) | `gas_line_present` ("confirmed there is no gas line present") is a *preventive* check for tasks where gas proximity is unknown (digging, drilling) — it's a non-sequitur once `task_text` already states gas is present/leaking/being connected, since presence isn't in question at that point. Those 3 examples' risk levels come entirely from the `gas_leak`/`fire` hazard tags and need no follow-up to resolve ambiguity that doesn't exist. `ml/data/README.md`'s field-semantics section updated to state this scoping explicitly so it isn't reintroduced during the 200–300-example expansion |
| 2026-07-29 | Added `tool_available:<tool>` as a second, distinct follow-up category (dynamic field name, not from the fixed 3-field safety-critical set) — asked whenever `tools_available` is unknown/unstated and the task genuinely needs a specific tool, per the user's explicit instruction | Distinguishes two flavors: purely functional tools (screwdriver — missing one blocks the task but doesn't change `risk_level`) vs. safety-relevant tools (a non-contact voltage tester — missing one means the user can't actually verify a safety-critical condition themselves, so it can co-occur with a hazard-confirmation follow-up like `power_isolated` on the same example, not replace it). Two demonstrative examples added (light-switch-plate and ceiling-fan variants, both with `tools_available: []`/tools genuinely unstated) |
| 2026-07-29 | Removed `followup_questions` entries from 3 examples where `task_text` already explicitly stated the answer (a user catch) — the confirmed-isolated and isolation-not-possible ceiling fan variants, and the load-bearing brick wall removal | A real system wouldn't (and shouldn't) ask a follow-up question the user already answered unprompted in their initial description — representing that as a Q&A pair implies a redundant round-trip that shouldn't happen. The answer is already fully conveyed through `task_text`/`risk_level`/`hazards`/`suggested_ppe`; no separate Q&A needs modeling. Contrast with genuinely-unstated cases (`"...not sure if it's load-bearing"`), which correctly keep `answer: null`. `ml/data/README.md`'s `followup_questions` section now states this explicitly so it isn't reintroduced during expansion |
| 2026-07-31 | Dropped `tools_available` as a model feature and retrained both models, discovered while wiring the classifier into the backend | `Job` has no tools column and intake never asks, so the backend can never supply it — training on it was straightforward train/serve skew. Measured before deciding: fitted with tools scored macro-F1 0.613, but served with tools blanked (all production could do) it fell to 0.549, so the model was genuinely leaning on a feature that would always be absent. Retrained both models without it for parity. **This materially changed Phase 4's conclusion**: the embedding model went from decisively worse (−0.075 ±0.033, losing 5/5 folds) to indistinguishable (−0.009 ±0.056). The baseline still ships, but now because the tie is broken on deployment cost rather than because it is more accurate. Phase 3/4 records updated rather than left stale. |
| 2026-07-31 | Classifier failures raise rather than returning a fallback risk level; out-of-range predictions are NOT clamped | CLAUDE.md forbids silently falling back to a 'safe' result on AI pipeline failure. Clamping an out-of-range prediction into 1–5 would look defensive but would hide a broken model behind a plausible number — failing surfaces it, and `assess_job` already turns the exception into `status=failed` at risk 5. Startup warmup is the exception: it logs and returns False rather than raising, because auth/intake/follow-ups still work without the classifier; `/health/ready` reports the degraded state. |
| 2026-07-30 | Extracted `ml/evaluation.py` as a shared harness BEFORE building the Phase 4 model, rather than letting each trainer compute its own metrics | Phase 4's exit check is a decision about which model *ships*. If the two scripts each defined their own metrics, a subtle difference in (say) how high-risk recall was collapsed would silently decide it, and the comparison would be unfalsifiable. Verified behaviour-preserving: baseline metrics byte-identical before and after the refactor. |
| 2026-07-30 | Phase 4: **TF-IDF baseline ships, embedding model rejected** — a documented negative result | Decision made on paired grouped 5-fold CV (identical folds, both models), not the held-out test set, whose ±0.2 CI at n=40 cannot separate two models. Embedding lost macro F1 by 0.075 ± 0.033 across 5/5 folds and was no better on high-risk recall (−0.020, inside the ±0.082 spread). Cause: risk here is carried by rare domain tokens (*gas*, *asbestos*, *load-bearing*, *consumer unit*) that TF-IDF weights precisely, whereas MiniLM's 384 general-similarity dimensions place *"replace a light fixture"* near *"replace a consumer unit"* despite three risk levels between them — and ~390 rows is too few to learn a head that recovers it. Shipping it would also add torch + a ~90 MB download for a measurable accuracy loss. Both artifacts kept per architecture.md §1. |
| 2026-07-30 | Phase 3 baseline: features restricted to `task_text`/`category`/`user_skill`/`tools_available`; `professional_category`, `suggested_ppe`, `hazards` and follow-up state all excluded as label leakage | Not a precaution — measured. `professional_category is None` matches `risk <= 2` in 555/555 rows, `suggested_ppe == []` in every risk-5 row, unanswered safety follow-up implies risk 5 in all 59 rows, and 37/66 hazard sets map to one risk level. Any of them would have produced a near-perfect, meaningless score. Follow-up state is doubly excluded because escalating on it is the rule engine's job under `final_risk = max(ML, rules)`; feeding it to the classifier would make the ML half relearn the rule half. |
| 2026-07-30 | Re-stratified the train/val/test split on problem-report style in addition to risk level, after the first baseline run predicted `safe_diy` for 16 of 24 truly-dangerous test rows | Error analysis, not inspection, found the cause: emergency/problem-report phrasing ("my gas water heater is hissing") is a small, stylistically distinct subpopulation that is almost entirely risk 5, and risk-level stratification alone put 10 in train against 14 in test — the test set held more of it than training did, so the pattern was unlearnable. A sparsity hypothesis was tested first and disproved (the failing rows had *more* nonzero features, 154 vs 132). Changing a split after seeing model output risks tuning to the test set, so the justification is structural — a subpopulation at 42/58 train/test makes both sides unrepresentative regardless of score — and `make_splits.py` now fails outright if train holds <50% of them. Splits went 387/86/82 -> 387/87/81; baseline macro-F1 0.533 -> 0.613. |
| 2026-07-29 | Expanded the seed dataset from 41 to **256** examples in three category batches, each validated before the next | Written via small generator scripts holding hand-authored example specs, rather than raw JSON: purely mechanical fields (`risk_label`, canonical question wording) get derived instead of retyped, so they cannot drift, while every judgement field (`risk_level`, `hazards`, `suggested_ppe`, which follow-ups apply) stays hand-authored per example. Wrote `ml/validate_dataset.py` first so each batch was checked against every documented rule rather than eyeballed — it caught a pre-existing wording bug (`"Do you have a voltage_tester available"`, underscore leaking into prose) on its very first run. One scoping rule was clarified during expansion: `power_isolated` applies to working on **fixed wiring**, not to any incidental `electrical_shock` risk — "isolate the circuit at the breaker" is meaningless advice when the risk comes from a wet tile saw's own supply, so those tasks carry `cuts_lacerations`/`hearing_damage` and no follow-up. Hazard taxonomy extended with `asbestos_exposure`, `heavy_object_handling`, `hearing_damage`, `confined_space`; a PPE vocabulary was added and is now validator-enforced |
| 2026-07-29 | Ran a full automated + manual consistency sweep across all 40 seed examples (risk_level/risk_label mapping, category/skill enums, `followup_questions` field/answer semantics, `suggested_ppe` at `risk_level: 5`), at the user's request — found and fixed one more issue: `"there's a live wire sparking near a water leak..."` (risk_level 5) was the only `dangerous`-tier example with non-empty `suggested_ppe` (`["insulated_gloves"]`), inconsistent with the other 3 risk-5 examples and arguably unsafe advice on its own (implies handling an active spark is okay if gloved, when the correct action is evacuate/cut power without touching it and call for help) | Fixed to `suggested_ppe: []`. Added an explicit rule to `ml/data/README.md`: `risk_level: 5` examples must always have `suggested_ppe: []` — at that tier the message should be "stop, don't attempt, call for help," not "here's what to wear to do it yourself." All other automated checks (label/level mapping, enum validity, low-risk-shouldn't-have-professional_category) passed clean across all 40 examples |
| 2026-07-29 | Confirmed `user_skill` doesn't need a `tool_available`-style follow-up when `task_text` doesn't restate it — user explicitly agreed to leave as-is after the distinction was explained | Unlike `tools_available`/hazard confirmations, `user_skill` is never inferred from `task_text` in the real app — it's a separate, mandatory quick-reply asked upfront (`app/chat/page.tsx`'s `ask_skill` stage) and a required field on every job (`schemas/job.py`). There's no real scenario where it's ambiguous/unaddressed the way `tools_available` can be, so each seed example's `user_skill` value represents a plausible real user's selection for that scenario (deliberately chosen to fit the narrative and `srs.md` §9's skill-based escalation rule), not a guess standing in for missing info |
| 2026-07-29 | Extended the redundancy rule (2026-07-29 entries above) one step further, per a sharp user catch: an explicit **"not sure if X"** statement in `task_text` is itself already an answer (an explicit "unconfirmed"), not silence — re-asking the identical follow-up would just get the same "I don't know" again. Removed `load_bearing_confirmed` from the two wall-removal examples whose `task_text` says "not sure"/"not totally sure" about that exact fact; added a new example (`"remove a wall between the kitchen and living room to create an open floor plan"`, load-bearing status never mentioned at all) so the field still has one live "genuinely unaddressed" demonstration | The true open-question case is now reserved strictly for when `task_text` says **nothing** about the topic — not "confirmed," not "denied," not even "unsure." `gas_line_present` on the gas-meter wall-removal example was left alone (proximity to a meter isn't the same as an explicit statement about the gas line itself, still a genuine open question); `tool_available:<tool>` follow-ups were also left alone (a blanket "not sure what tools I have" is a general disclaimer, not a direct answer to whether one specific named tool is available). `ml/data/README.md`'s `followup_questions` section rewritten to state the refined rule and both contrasting examples explicitly |
| 2026-07-29 | Escalation rule added: any dataset example with an unanswered (`answer: null`) safety-critical follow-up (`power_isolated`, `load_bearing_confirmed`, `gas_line_present`) must have `risk_level: 5` (not just `4`), per explicit user instruction | Directly follows `CLAUDE.md`'s existing non-negotiable rule ("missing safety-critical follow-up answers escalate risk, treat as worst plausible case, never assume safety"), applied literally — not knowing whether a safety-critical condition holds rules out nothing, so it's escalated to the max rather than to "professional required." Applied to 8 of the 41 seed examples (all `risk_level` bumped from 3/4 to `5`, `suggested_ppe` cleared to `[]` per the existing risk-5 rule, `professional_category`/`hazards` left unchanged). Does **not** apply to `tool_available:<tool>` follow-ups (capability, not hazard severity) or to hazard-confirmation follow-ups with a non-null answer (a *known* state, e.g. "isolation confirmed not possible but mitigated via PPE," which is scored on its own merits and can land anywhere on the scale — see the `"install a ceiling fan..."` variants). **This is stronger than the current Phase 6 placeholder rule engine** (`ai/rule_engine/rules.py`'s `_SAFETY_CRITICAL_FOLLOWUPS` only escalates to level 4) — worth carrying forward explicitly when Phase 5 replaces it; `ml/data/README.md` documents the rule and this gap |
| 2026-07-29 | Full review pass over all 38 seed examples for the same class of mismatch (a follow-up field or hazard tag reused in a context its wording doesn't fit), requested by the user after the gas catch above | Found and fixed 2 more: `load_bearing_confirmed` ("confirmed NOT load-bearing?") was attached to a foundation-wall crack *repair* and a new deck *build* — neither is a removal/demolition decision, so there's no "is this load-bearing?" question to resolve even though both correctly carry the `structural_collapse` hazard tag; removed the follow-up from both (`followup_questions: []`), kept the hazard tag and risk_level. Also found the `"dig a small trench..."` example (added the same day) mistagged its hazard as `gas_leak` (an active-leak tag) when the actual risk is *striking* an unknown line, not a leak — added a new taxonomy tag `buried_utility_strike` for this distinct case and retagged it. `ml/data/README.md` updated in both places (the `load_bearing_confirmed` scoping note, the hazard taxonomy list) so the distinction isn't lost during expansion |

| 2026-08-01 | **The LLM may now select which follow-up questions to ask, from a closed catalog (`LLM_SELECTABLE_FOLLOWUP_FIELDS`), and every hazard tag must quote the user's own words.** Discussed at length with the user before implementing; the user proposed the direction ("the LLM should not assume anything — whatever is ambiguous it should mark as a follow-up question") | The boundary that mattered: the LLM **cannot invent a question**, only select one that already exists in `catalog.FOLLOWUPS`, and selection is **additive** — whatever `required_followups()` derives from fired hazards is asked regardless. That keeps `rules.md` §4 intact, because follow-up answers drive escalation (`floor_when_missing=5`) and a model that could *suppress* a question would be indirectly deciding risk. Evidence grounding is the other half: a tag now carries an `evidence` span verified as a literal substring of the description, so a hazard the model merely inferred is discarded mechanically. Prompt prose alone had already failed at this — the previous prompt said "do not tag a hazard the description does not report" and still produced `work_at_height` for a light bulb |
| 2026-08-01 | **Added `Rule.gated_by`: a confirmed-safe follow-up answer can stop a rule firing.** Explicitly checked against the never-de-escalate invariant before building | This is **not** de-escalation and `evaluate()` still has no path that lowers a value. A gate refines the *trigger condition* — it is `excludes` sourced from a user answer instead of from description text — so the rule simply never fires; nothing already assigned is reduced. The safety property that makes it sound is the default: an **unanswered** gate still fires the rule at its full floor, so silence never buys a lower risk level, only an explicit answer does. `HARD_GATE_RULE_IDS` (gas, live conductors, water-at-electrics, asbestos) are permanently ungateable — people are poor judges of those and being wrong is fatal — enforced by `test_catastrophic_hazards_can_never_be_gated_away` |
| 2026-08-01 | Considered and **rejected** three larger redesigns the user raised, in this order: (1) delete the rule engine and feed its inputs to the classifier; (2) replace the rule engine with a five-band tag→level rubric; (3) expand the tag catalog from 21 to "much, much more" | (1) fails because the classifier is structurally blind to `followup_answers`, and `train_baseline.py` already measured why feeding them in is worthless — "unanswered safety followup ⟹ risk 5" holds in all 59 such rows, so the model would memorise one rule and report a flattering number for work the rules already do. It also can't be deleted: `required_followups()`/`next_followup()` drive conversational intake, which a 1–5 integer cannot. (2) was found to be **already implemented** — `Rule.floor` *is* the tag's rubric level and `risk = max(risk, floor)` *is* "highest matched band wins"; rewriting the catalog as a five-band table would be a re-spelling, and crucially would not have fixed the reported bug, which was upstream in what the tagger emitted. (3) partly accepted: `work_at_height` was genuinely too coarse and was split, but a blanket expansion was declined — the tagger prompt already inlines every rule's id, summary, explanation and keywords, selection accuracy degrades as the menu grows, and neither bad tag in the reported bug was caused by a missing tag |

| 2026-08-01 | **`RISK_USE_ML_CLASSIFIER=false` in dev — the risk pipeline is running RULES-ONLY.** User's explicit decision ("let's pause the classifier for now and use only the rule engine") after live-testing "how do i change my light bulb" as an Experienced user and getting **Professional Required**, with `ml_risk=4, rule_risk=1, final_risk=4` in `ai_logs` — the deterministic half said harmless, the ML half decided the answer | Implemented as a **config flag, not a deletion**: the classifier still runs, still writes `ml_risk`/`ml_confidence` to `ai_logs`, and is merely excluded from the `max()`. Default is **true** so the documented pipeline stays the code's default and only this machine's `.env` differs. **The cost was stated before implementing and is not cost-free:** removing a term from a `max()` can only lower the result, so a task matching no keyword and receiving no LLM hazard tag now returns level 1 unopposed — the rule engine's known blind spot, for which the classifier was nominally the cover. Judged acceptable only because that cover is currently fictional (see the 🔴 Known Issue): the classifier returns 1 for a Beginner on "rewire the consumer unit" and 4 for an Experienced user on a paint roller, so it was contributing noise in both directions rather than protection. Two secondary decisions: `_build_explanation` no longer says "Classifier predicted risk level N" when the classifier had no vote (a false explanation of a safety decision is worse than a terse one), and a classifier crash no longer fails the assessment in this mode (it cannot lower a result it was never counted in) but is recorded as `ml_error` rather than swallowed. **If this becomes permanent rather than temporary, `architecture.md` §2, `srs.md` §8.1 (NFR-03) and `CLAUDE.md` all need updating — they currently state `max(ML, rules)` as non-negotiable, which is true of the default but not of this machine** |

| 2026-08-01 | **Catalog expanded 21 → 33 rules, closing six hazard families that had no rule at all.** Found by auditing the catalog's `Rule.hazard` values against the `hazards` taxonomy the dataset actually uses: the catalog covered 9 of 16 families, and `respiratory_hazard` (90 rows), `cuts_lacerations` (71), `fire` (50), `heavy_object_handling` (45), `burns` (18) and `hearing_damage` (10) had none | The original 21 rules were all acute-catastrophic — gas, electrocution, collapse, falls, asbestos, confined space, buried services — with nothing for dust/fumes, blades, ignition, lifting, burns or noise. That is why keyword rules under-called 37.7% of high-risk rows, and why the tagger routed a blowtorch to `asbestos_disturbance`: it was picking the best option from a menu that did not contain the right answer. New rules: `hot_works_ignition`, `flammable_vapour_enclosed`, `appliance_flex_overload`, `silica_dust`, `lead_paint_disturbance`, `refrigerant_circuit_work`, `sewage_contamination`, `tree_felling`, `powered_cutting_tool`, `heavy_manual_handling`, `sustained_noise_exposure`. **Measured effect on the 555-row dataset (keyword rules only, no LLM): high-risk recall 0.623 → 0.681; rows falling all the way to level 1 while truly 4-5: 32 → 20; over-escalation cost 3 rows out of 242 truly-low-risk ones.** That last number is the one to watch on any future expansion — this layer's value is that it almost never false-fires, and loose single-word keywords would spend it. `sustained_noise_exposure` deliberately has **floor 1**: hearing damage does not change who should attempt a task, so escalating for it would be wrong under the competence ladder, but the rule still fires and still reaches `explain()`/PPE — floor 1 is how the catalog says "worth telling the user, not worth escalating" |
| 2026-08-01 | Decided **not** to pursue "add rules until the catalog is exhaustive" as a replacement for the classifier, after the user asked whether that would suffice | Exhaustiveness is achievable over **hazard families** (a finite list, ~20-25) but not over **tasks or their phrasings**, which is where the residual sits. After the expansion, the 20 remaining level-1 misses split into three groups, none fixable by more keywords: (a) ~6 are label artifacts — the ceiling-fan cluster is labelled 5 only because of the "unanswered follow-up ⟹ 5" rule, and scores 3 correctly once answered; (b) most of the rest are *phrasing* gaps inside families the catalog already covers ("install an outdoor floodlight", "cut a new doorway through a stud partition", "knock out part of the basement wall") — the hazard has a rule, the wording just misses it, which is precisely what the LLM tagger exists to bridge; (c) one genuinely new family (pressurised hot-water/scalding systems). Measured LLM contribution on the hardest subset: of 29 distinct tasks scored 1 by keyword rules while truly 4-5, tagging rescued 9 to ≥4 and partially escalated 9 more. Also relevant to the decision: **both providers rate-limited during a single ~30-row evaluation** (Groq daily cap, then Gemini `RESOURCE_EXHAUSTED`), so with the classifier disabled, LLM availability is system availability — a local model has no quota. Conclusion recorded: keep the hybrid, fix the classifier's confound rather than delete it |

| 2026-08-01 | **Fixed the `user_skill`/`risk_level` confound by rebalancing the ANNOTATION, not the labels** (`ml/rebalance_skill.py`, run with `--apply`). NMI 0.657 → 0.000; the four empty (skill, level) cells are gone | The confound could be fixed from either side — correct the labels that were set with an eye on the narrator, or correct the narrator. Correcting labels would mean re-judging 256 hand-authored safety assessments with no domain-expert baseline (`ml/data/REVIEW.md`), substituting one person's judgement at scale for another's. Correcting the annotation changes nothing anyone relied on: **measured first, zero of the 256 seed `task_text` values mention the narrator's experience**, so `user_skill` had no anchor in the text and a row reads identically whoever is asking. Method is round-robin within each risk level over rows sorted by `id` — deterministic, no RNG, so the diff is reviewable and reproducible. 170/256 seeds changed; 200/299 generated variants re-synced to their parent (all 299 already tracked their parent exactly). **`risk_level`, `risk_label`, `hazards`, `task_text` and follow-ups: zero changes, by construction.** `ml/validate_dataset.py` now fails the build if NMI > 0.05 |
| 2026-08-01 | **Re-ran the Phase 4 baseline-vs-embedding comparison on the clean labels. The original decision (ship TF-IDF) survives** — the hypothesis that it was an artifact of the confound was WRONG | The suspicion was reasonable: Phase 4 found the two "statistically indistinguishable", but both models could reach ~0.61 by reading the `user_skill` one-hot, so the comparison plausibly measured the confound rather than the features. Re-run on rebalanced data, paired grouped 5-fold CV over identical folds: macro-F1 embedding **0.338** vs baseline **0.349** (diff -0.010 ± 0.039, embedding wins 3/5 folds); high-risk recall embedding **0.589** vs baseline **0.647** (diff -0.059 ± 0.087, embedding wins **1/5** folds). Embeddings are marginally worse overall and clearly worse on the safety metric. **What this rules out:** local semantic generalisation is not available for free by swapping TF-IDF for frozen `all-MiniLM-L6-v2` at this data scale — so the LLM tagger remains the only source of genuine semantic generalisation, and the token-cost problem is structural rather than a wrong-tool problem. **What it does NOT rule out:** 387 training rows is very small for 384-dim dense features, and a larger encoder, a domain-adapted one, or fine-tuning rather than frozen-embeddings-plus-LogReg were not tested. This is the experiment to re-run if the dataset is ever scaled |
| 2026-08-01 | **A finished chat now refuses further input rather than silently starting a second job.** User-reported: typing anything after a risk card appeared spawned a new sidebar entry | One chat = one job = one assessment, and the backend already enforces the last link (`risk_assessments.job_id` is unique) — the frontend was the only place that pretended otherwise, running its whole intake flow again inside a conversation that already had a verdict. Fixed at the stage level rather than by hiding the composer: `handleSend` refuses in stage `done` (covering example chips and quick replies too) and the composer is disabled with an inline "New chat" prompt, so the rule is visible instead of a click that does nothing. Required mirroring `flowStageRef` into state (`setStage` writes both) — the ref exists because async handlers read the stage synchronously mid-flow, but the composer has to re-render when the flow ends |
| 2026-08-01 | **Deleting a job deletes its `ai_logs` rows too**, rather than keeping the audit trail orphaned or blocking the delete | rules.md §4 point 6 requires that every assessment attempt *is* logged with no sampling; it does not require the log to outlive the user's own task. Those rows embed the task description, so retaining them after a user deletes the conversation would leave exactly the user data behind that they asked to be rid of. Also the practical constraint: `ai_logs.job_id` is NOT NULL with no cascade, so the alternatives were "delete them" or "refuse to delete the job at all" |

## Bugs Found & Fixed

_(Real runtime bugs caught during verification, not just typos — kept here since they're non-obvious enough to bite again if similar patterns are reused. Known pitfalls specific to the current frontend implementation are documented inline in `design.md`'s ⚠️ callouts instead of here.)_

| Date | Bug | Root cause | Fix |
|---|---|---|---|
| 2026-07-19 | Homepage showed "Backend: unreachable — Failed to parse URL from /health" with the backend actually running | No `apps/frontend/.env.local` existed (only `.env.example`), so `NEXT_PUBLIC_API_URL` was `undefined` at runtime; the root page's server-side `fetch()` can't resolve a relative URL without a base | Created `.env.local` from `.env.example`, restarted the dev server — confirmed via Chrome (claude-in-chrome) that `/` now shows "Backend: ok" |
| 2026-07-31 | With Groq live, "install a new light fixture in my hallway" (Beginner, power confirmed isolated) came back **Dangerous/level 5**, citing explanations about live supplies to a user who had just said the power was off. Reported by the user as "if the power is off, some of the risks are prevented" | Not a floors problem — an LLM over-tagging problem. Keyword rules alone score the task 3; the tagger added `exposed_live_conductor` (**floor 5**) and `supply_side_electrical` (floor 4). Both describe something the task isn't: the former's keywords are "live wire", "sparking", "arcing", "burning smell" — an *active fault the user reports* — and the latter is consumer-unit/meter work. The tagger only ever saw the one-line summary, and "Exposed conductors of unverified status" reads as covering anything that momentarily exposes a wire | The prompt menu now carries each rule's full explanation plus example wording, and the instruction separates hazards **inherent to the work** (a light fitting IS fixed-wiring work — tag it) from hazards describing **a different location or an unreported fault** (don't). No floor and no risk arithmetic changed; keyword rules still fire independently, so this is precision only. **Two traps hit while fixing it, both worth remembering:** (1) a first attempt that only preached caution made every model tag *nothing*, silently undoing the ceiling-fan fix — over-correcting a safety prompt is as harmful as under-correcting; (2) the richer multi-line menu made ids less salient and models began returning plausible paraphrases (`fixed_wiring` for `fixed_wiring_work`, `working_at_height` for `work_at_height`) which `VALID_RULE_IDS` correctly discarded and logged, so everything came back untagged while looking healthy. Ids are now quoted, required verbatim, and the valid set is listed. The catalog filter behaved exactly as designed throughout — it was the only reason a hallucinated id never reached the engine |
| 2026-07-31 | Starting a new chat injected "This assessment wasn't finished — continue?" into the *middle* of the live conversation. Caught in Chrome while verifying transcript replay — the feature's own first browser test | The chat-restore effect re-read `localStorage` on every `jobsById` change. Creating a job calls `rememberActiveJob(job.id)`, so by the time the new job landed in `jobsById` the pointer had been overwritten with the id of the chat already on screen — the effect then "restored" the current conversation on top of itself, and since the job was still `pending_followup` it appended a resume prompt mid-thread | Capture the pointer **once at mount** into a ref, and abandon the restore entirely if `flowStageRef` has already left `"idle"` — a late-arriving job list must never overwrite a conversation in progress. Re-verified in Chrome: the resume prompt now appears only at the end of a genuinely unfinished chat |
| 2026-07-31 | The app looked signed in but every request failed with "Invalid or missing authentication credentials", and the sidebar read "No saved conversations" — indistinguishable from data loss. Found live in Chrome while verifying chat persistence | The JWT had expired. The chat page's auth gate only checks that a token **exists** (`if (!getToken()) router.replace("/login")`), never that it works, and the displayed user name comes from `localStorage` — so a dead session renders a fully populated shell in which nothing functions. Every failure surfaced as an in-chat error bubble instead of a re-login prompt | `lib/api.ts` now treats any 401 as session-over at the single fetch chokepoint: clear the token and redirect to `/login`. `/auth/*` is excluded so a wrong password still renders inline on the login form instead of looping the page on itself. Verified in Chrome: reloading `/chat` with the dead token now lands on `/login` |
| 2026-07-31 | User submitted "i want to replace my ceiling fans" (Beginner) and got **Dangerous/level 5** citing `missing_followup:power_isolated` — for a question the chat flow **never asked them**. Reported verbatim: "the question was never asked how am i supposed to answer it" | The LLM hazard tagger ran **only inside `assess_job`**, but the follow-up gate (`job_service._missing_required_followups` → `rules.required_followups`) was called **without** `llm_hazard_ids`, so it saw keyword matches only. "ceiling fan" is not among `fixed_wiring_work`'s keywords, so at intake: no hazard → no required follow-up → `status="ready_to_assess"` → nothing asked. At assessment the tagger fired `fixed_wiring_work`, which made `power_isolated` required, unanswered, and worth its `floor_when_missing=5`. Two derivations of the same hazard set, one deciding what the user is ASKED and one deciding how they're SCORED — exactly the drift `rules.py`'s "single source" comments claimed was impossible. Reproduced deterministically: `required_followups(desc, 'electrical', None, 'Beginner') == []` vs `required_followups(desc, 'electrical', ['fixed_wiring_work'], 'Beginner') == ['power_isolated']` | Hazard ids are now resolved **once, at job creation**, and persisted on new column `jobs.llm_hazard_ids` (migration `e5f7c1a92b40`); the gate, the question prompt and `evaluate()` all read that one column via `job_service._hazard_ids`, so they cannot disagree. NULL = "not tagged" (Gemini down at creation) vs `[]` = "tagged, no hazards" — `llm_assist.tag_hazards_result` was added to preserve that distinction, and `ensure_hazard_ids` retries tagging on the next `followup`/`assess` call. A hazard found by that retry produces a 409 `followup_incomplete` (ask the question) rather than a missing-answer penalty; the chat UI now recovers from that 409 by re-reading the job and asking, instead of dead-ending. **Note this only removed the phantom penalty — the rule floor for this task is 2, but the ML classifier independently predicts 5 for a beginner, so the task still rates Dangerous. That is now an honest classifier judgement rather than a fabricated unanswered question.** Files: `models/job.py`, `alembic/versions/e5f7c1a92b40_*.py`, `services/job_service.py`, `ai/rule_engine/llm_assist.py`, `apps/frontend/app/chat/page.tsx`, `tests/test_llm_job_flow.py` (2 regression tests) |
| 2026-08-01 | User asked "how do I change my light bulb" (Beginner, power confirmed off) and got **level 3**, citing fixed-wiring and fall-from-height explanations. Reported verbatim: "the first reason doesn't make sense because I just said I turned off the electricity, and the second reason also doesn't make sense because I never said how high the light bulb is" | **Neither rule keyword-matched — both came from the tagger.** "light bulb" appears in no keyword list; `fixed_wiring_work` (floor 2) and `work_at_height` (floor 3) were both LLM-proposed, and the level-3 floor came entirely from the latter. Two distinct causes. (a) The prompt written on 2026-07-31 to stop *under*-tagging says "Replacing a light fitting, a ceiling fan or a socket IS work on fixed wiring" — correct for a fitting, but the model generalised one hop to a *bulb*, which is a user-serviceable consumable, not part of the installation. (b) Nothing checked whether a tag was grounded in anything the user said, so the model reasoned bulb → ceiling → overhead → height and tagged a hazard from an assumption. **A third, structural cause made both worse: LLM-proposed ids bypassed every catalog condition.** `matched_rule_ids` checked membership in `VALID_RULE_IDS` and nothing else, so an LLM tag skipped `excludes`, `categories` *and* `requires_skill` — `work_at_height` lists "step ladder" and "ground level" in its excludes and the tag ignored both; `electrical_work_by_beginner` is skill-gated and a tag would have fired it for an expert. The docstring promised those vetoes applied; for half the ways a rule could fire, they did not | Four changes. **(1) Evidence grounding:** `HazardTag` now carries an `evidence` span, verified as a literal substring of the description (`_evidence_supports`); an ungrounded tag is discarded and logged. A model cannot quote words the user never wrote, which closes the inference path mechanically rather than by prompt wording. **(2) Gates applied to LLM tags:** `_matches` was split into `_gates_allow` (categories/skill/excludes/gate) + the keyword test, and LLM ids now pass `_gates_allow`. Keywords stay out of that check — catching what keyword matching missed is the whole point of tagging — and a keyword match is still never suppressed, so tagging remains additive. **(3) Uncertainty becomes a question:** the tagger may put a follow-up field in `ask` (closed set, additive, persisted on new column `jobs.llm_followup_fields`, migration `b9d5a3e17c62`) instead of guessing. New gated rule `overhead_work_unknown_height` (floor 3, no keywords, LLM-tag-only) covers "plainly overhead but height unstated": it fires while `height_access` is unanswered and stops firing once the user confirms the work is reachable from floor level. **(4) Prompt:** the light-fitting guidance is kept but now explicitly excludes consumables (bulb, filter, battery, fuse in a plug). Verified end-to-end: the bulb case now asks about height, then assesses at **level 1**. 9 new rule-engine tests + 2 new integration tests |
| 2026-08-01 | **Found while writing the above, not reported:** the classifier silently returns a different risk level for the same job depending on the *casing* of `skill_level`. Measured on the shipped artifact: "how do i change my light bulb" scores **1** with `user_skill="Beginner"` and **5** with `"beginner"` | Textbook train/serve skew. The `OneHotEncoder` is `handle_unknown="ignore"`, and the dataset contains exactly three skill strings (`Beginner`, `Some experience`, `Experienced`) and nine lowercase categories. Any value outside that vocabulary emits an all-zero block instead of erroring, so the prediction quietly falls back to the task text alone — a four-level swing with nothing logged. `skill_level` is a free `str(min_length=1, max_length=50)` at the API boundary (`schemas/job.py`), so nothing enforces the vocabulary; only a UI convention was keeping them aligned (`app/chat/page.tsx` happens to send the three exact strings as quick-replies, which is why this never bit in production and why the reported bulb case did show classifier=1) | `classify()` now snaps `category`/`user_skill` onto the trained vocabulary case- and whitespace-insensitively, reading that vocabulary off the fitted encoder (`_trained_vocabularies`) rather than hardcoding a second copy that could drift from the artifact. A genuinely unknown value is passed through unchanged and logged at WARNING — guessing which trained value the caller meant would be a silently invented input to a safety decision. 2 regression tests in `test_classifier.py`. **Note this only removes the casing cliff; see Known Issues for the much larger problem this uncovered about what the classifier is actually keying on** |
| 2026-07-18 | `apps/frontend` build failed ("Couldn't find any `pages` or `app` directory") when starting Phase 7 work | All route files (`layout.tsx`, `page.tsx`, `globals.css`, `(auth)/login\|register`, `chat`, `dashboard`) were sitting directly under `src/` instead of `src/app/` — despite the Phase 0 frontend/QA agents both reporting they were under `src/app/...` and verifying a live build. Unclear how the `app/` segment was lost (never committed to git, so no history to diff); possibly a mis-execution during the earlier session's cleanup step | Moved all 7 files/dirs into `src/app/` manually, re-ran `npm run build` — all 6 routes (`/`, `/chat`, `/dashboard`, `/login`, `/register`, `/_not-found`) compile again |

## Known Issues / Open Threads

- **🔴 The tagger prompt has outgrown the free-tier rate limits, and it scales with catalog size.** Measured 2026-08-01 after the 21 → 35 rule expansion: the prompt inlines every rule's id, summary, **full explanation** and six keywords, and now runs **4,209 tokens per call**. Against Groq's free tier (12k tokens/minute, 100k/day, `llama-3.3-70b-versatile`) that is **~2 calls per minute and ~23 job submissions per day** — not a usable product, and it degrades silently to keyword-only rather than erroring. A fresh Groq key was exhausted within one 18-case evaluation sweep. Breakdown of the 13,599-char hazard menu: explanations **49%**, keywords 20%, summaries 11%. A compact format (id + summary + 5 keywords) is 64% smaller. **Do not simply strip the explanations** — they were added on 2026-07-31 to fix a real scoping bug (the model read "Exposed conductors of unverified status" as covering any job that briefly exposes a wire and tagged a routine light-fitting swap with a floor-5 rule), and removing them would regress that. Options not yet chosen: two-stage tagging (cheap shortlist by family, then full explanations for only the shortlisted rules), sending explanations only for rules whose keywords or category are plausibly in play, or provider/model change. **This is now the binding constraint on catalog growth: every rule added improves deterministic coverage AND reduces how much LLM tagging is affordable.**
- **Groq and Gemini disagree materially on the same input, and with the classifier disabled that difference lands directly on the user.** On "clean the gutters using a ladder" (2026-08-01, 35-rule catalog): Gemini tags `work_at_height` → level **3**; Groq tags `work_at_height` + `overhead_work_unknown_height` + `unprotected_or_adverse_height_work` → level **5**. That last rule is for wet/icy roofs and explicitly-stated absence of a harness; Groq inferred it from a routine gutter clean. Note this passed the evidence check — the model quoted real text — so **evidence grounding prevents invented facts but not mis-routing of real ones**, the same failure class as the blowtorch tagged `asbestos_disturbance`. Groq over-escalates height work; Gemini scored 16/18 exact on the comparison set against Groq's 5/18 (12 of Groq's were rate-limited, so that comparison is not yet clean — re-run when the prompt is smaller).

_(Anything left dangling — a TODO, a bug, a question for the supervisor.)_

- **🔴 The shipped classifier appears to key almost entirely on `user_skill`, not on the task.** Uncovered 2026-08-01 while fixing the casing bug above; **not yet addressed, and it is the largest open accuracy problem in the system.** Measured directly against `ml/eval/baseline_model.joblib`, holding the task text fixed and varying only skill:

  | task (matched category) | Beginner | Some experience | Experienced |
  |---|---|---|---|
  | how do i change my light bulb | 1 | 5 | 4 |
  | paint a bedroom wall with a roller | 1 | 2 | 4 |
  | assemble a flat pack wardrobe | 1 | 2 | 4 |
  | rewire the consumer unit | **1** | 2 | 4 |
  | I can smell gas near the cooker | 5 | 5 | 4 |

  Two things are wrong here and they compound. The prediction barely moves with the task — "rewire the consumer unit" and "assemble a flat pack wardrobe" both score **1** for a Beginner — and the skill axis runs **backwards**, with `Experienced` scoring 4 on everything including a bulb, while `Beginner` scores 1 on almost everything including consumer-unit work. Changing a bulb rates **5** for "Some experience". This is consistent with the documented macro-F1 ≈ 0.55–0.61 and high-risk recall ≈ 0.65, but the failure mode is far more specific than those aggregate numbers suggest, and it means **the rule engine is currently carrying essentially the entire safety decision** — `max(ML, rules)` is doing nothing useful when the ML term is a near-constant function of a dropdown. Likely causes to investigate, in order: `class_weight="balanced"` interacting with a small corpus where skill correlates with label by construction (seed examples chose `user_skill` "to fit the narrative" — see the 2026-07-29 decision entry, which explicitly reasoned this was safe); 299 of 555 rows being weakly-labelled paraphrases; and one-hot skill being 3 dense columns against a sparse high-dimensional TF-IDF block. Worth a `ml/analyze_recall.py`-style ablation (fit without `user_skill` and compare) before any retraining. Do **not** treat the classifier's output as meaningful until this is resolved.
- **`docs/` folder not created.** architecture.md's target repo layout puts all 7 markdown spec files under `docs/`; they currently still live at repo root. Needs a user decision: move them (updates every cross-reference in this file, CLAUDE.md, and the docs themselves) or update architecture.md to reflect root-level docs as the real convention. Not done automatically.
- **Stale path string in `apps/frontend/src/app/page.tsx`** — footer text references `docs/phases.md` / `docs/architecture.md`, which don't exist yet (files are at repo root). Cosmetic only, no functional impact; fix whenever the `docs/` question above is resolved.
- ~~**Postgres not yet confirmed running**~~ **RESOLVED** — see the port 5432 entry below: `buildsafe-postgres` is running, reachable, migrated, and confirmed serving real requests as of 2026-07-19.
- **Phase 1's open questions were resolved provisionally (2026-07-19), not by the supervisor** — no supervisor was available. `prd.md` §7 now carries working targets for dataset size/expert-review process and false-negative tolerance, both explicitly marked "pending supervisor confirmation." Re-open and update `prd.md`/`phases.md` if the supervisor gives different numbers once available.
- **`apps/backend/routers/recommendations.py` route shape is a guess** — architecture.md only specifies a top-level `/recommendations` prefix, not the exact path. Backend agent chose `GET /recommendations/{job_id}` as the most consistent placeholder; now implemented for real against that shape.
- **`ai/classifier/` and `ai/rule_engine/` are TEMPORARY placeholders, not the real Phase 3-5 work.** Real risk decisions from the backend right now are not trustworthy — the classifier is a keyword heuristic, not a trained model, and the rule engine is a small hardcoded keyword list, not the srs.md §9 catalog (and has no LLM hazard-tagging call). Must be replaced before Phase 6's exit check can be considered truly met, and definitely before any real Phase 8 deployment. Both invariants that matter (max(ML,rules), missing-followup-escalates) are enforced correctly in the wiring, so replacing the placeholders should be a drop-in swap of `classify()`/`evaluate()`'s internals.
- **`passlib`/`bcrypt` version pin is broken in the current environment** (`requirements.txt` has `passlib==1.7.4` + `bcrypt==5.0.0`, an incompatible pair) — `core/security.py` works around it by calling `bcrypt` directly instead of via `passlib.context.CryptContext`. Should be reverted once `requirements.txt` can be re-pinned with network access (passlib >=1.7.5 or bcrypt <4.1).
- **`email-validator` not installed** — `schemas/auth.py` uses a hand-written regex instead of Pydantic's `EmailStr` for the same offline-environment reason. Swap back once installable.
- **Disposable test Postgres container `buildsafe-postgres-test` (port 5433) is still running** from the Phase 6 build/verification session — holds no real data, safe to remove or keep for reuse in future test runs.
- ~~**`GEMINI_API_KEY` is unset in this environment**~~ **RESOLVED — a real key is present in `apps/backend/.env` as of 2026-07-31** (confirmed live: category inference returned `electrical` and follow-up wording came back Gemini-phrased, not the hardcoded fallback). Note the consequence found the same day: with tagging live, the LLM can surface hazards keywords miss, which is why the tagged set now has to be persisted per job (see the 2026-07-31 bug entry). Original note kept below for the fallback behaviour it documents.
- **(historical) `GEMINI_API_KEY` was unset** (`apps/backend/.env`'s value is empty) — no real Gemini key was available to either build or verification sessions. This is fully expected and non-blocking: `tag_category`/`phrase_followup_question` both have hardcoded, deterministic fallbacks (`"general"` category; fixed default question wording for `power_isolated`/`load_bearing_confirmed`/`gas_line_present`) that were exactly what live end-to-end verification exercised. Add a real key to `apps/backend/.env` (obtained from Google AI Studio, see `.env.example`) whenever more varied/accurate category inference and more naturally-phrased follow-up questions are wanted — no code changes needed, it's picked up automatically.
- **The Gemini key is currently returning HTTP 429 (quota exhausted), not failing outright** — confirmed 2026-07-31 via `python scripts/check_llm.py --verbose`. Effect is invisible from the UI: category/hazard tagging silently falls back to `"general"` / keyword-only rules and follow-up questions revert to their hardcoded wording, so assessments still work but are less accurate. Either wait for the quota window, add billing, or switch to Groq (`LLM_PROVIDER=groq` + `GROQ_API_KEY`). Re-run the script to confirm which state you're in.
- **The Groq provider path has never run against the live Groq API** — no key was available when it was written (2026-07-31). It's covered by 22 mocked unit tests including every failure mode, and it degrades to the same hardcoded fallbacks as any other outage, so worst case is "no better than no LLM". Still, first real use should be `python scripts/check_llm.py --verbose`, not a user-facing request. Two things to watch: whether `GROQ_MODEL` is still a valid id for your account (`--list` prints what your key can call) and whether it lands in `json_schema` or the `json_object` fallback mode (the log line says which).
- **Turbopack crashes on `npm run dev` on this machine.** `next dev` (Turbopack, the default) panics compiling `app/globals.css` with `Failed to write app endpoint /chat/page` / a spawned `node.exe` exiting immediately with `0xc0000142` (STATUS_DLL_INIT_FAILED) — confirmed unrelated to any app code (crash is in Turbopack's own PostCSS subprocess spawn; `lightningcss`/`@tailwindcss/oxide` both load fine in-process; only one `node.exe` on PATH; no stray env vars). Most likely Windows Defender/AV blocking freshly-spawned `node.exe` child processes, or a corrupted system DLL cache. Workaround in use: `npx next dev --webpack` (Webpack instead of Turbopack) — works cleanly, just slower rebuilds. Real fix (untried): add a Defender exclusion for `C:\Program Files\nodejs\node.exe` and this project folder, then retry plain `next dev`.
- ~~**Port 5432 conflict...**~~ **RESOLVED 2026-07-19 — for real this time.** Root cause: a native Windows service `postgresql-x64-18` (unrelated Postgres 18 install, `C:\Program Files\PostgreSQL\18`) was bound to port 5432, colliding with `buildsafe-postgres`'s Docker port-forward. First pass stopped the native service, which worked but broke the user's existing pgAdmin connection to their native Postgres data. **Real fix: `buildsafe-postgres` now publishes on host port `5434` instead of `5432`** (`docker-compose.yml`'s `ports: "5434:5432"`; `DATABASE_URL` in `apps/backend/.env` and `.env.example` updated to match) — both Postgres instances now run permanently side by side with no conflict, native service restored to normal (`Automatic` startup, running). Container was recreated on the new port (`docker compose up -d`); named volume preserved existing data (`users` table still had prior rows post-recreate). Verified: plain `uvicorn main:app --reload` (no env overrides) + `POST /auth/register` work end-to-end against `buildsafe-postgres` on 5434. Also discovered along the way that `alembic upgrade head` had never actually been run against this container before (nobody had reached it with correct credentials pre-fix) — applied now, `users`/`jobs`/`risk_assessments`/`ai_logs` tables confirmed present.
- **The disposable test Postgres container's schema does not persist across `pytest` runs** (worth knowing for future live/manual verification against it): `tests/conftest.py`'s session-scoped fixture runs `Base.metadata.drop_all` at teardown, dropping all real tables — but `alembic_version` (a separate bookkeeping table Alembic owns, untouched by `Base.metadata` operations) is left pointing at `head`, so a subsequent plain `alembic upgrade head` sees "already up to date" and is a silent no-op, leaving the container with an empty schema. If manually verifying against this container after a `pytest` run, either recreate tables directly (`Base.metadata.create_all` via a one-off script) or `alembic downgrade base && alembic upgrade head` won't work either (downgrade tries to drop already-gone tables) — dropping the `alembic_version` table first, then `alembic upgrade head`, is the reliable reset. This bit both the Phase-6 build session and this session's verification.
- **On Windows/Git-Bash, `pkill -f <name>` does not reliably kill Python/uvicorn processes started via `nohup ... &`** — they appear to run as native Windows processes invisible to Git Bash's `ps`/`pkill` (MSYS2's process view), so a "restarted" server can silently still be the old process bound to the port, serving stale code. Use `netstat -ano | grep :<port>` to find the real PID, then `Stop-Process -Id <pid> -Force` via PowerShell to actually kill it. Cost real time during this session's verification (a fix appeared to not take effect because the old process was still answering requests).

- **The Phase 6 placeholder rule engine's `power_isolated` follow-up is binary and can't express "isolation not possible, but mitigated via PPE" as distinct from "isolation simply unanswered."** Surfaced during Phase 2 dataset design (2026-07-29, see Decisions Log) — the dataset now models three states for electrical tasks (isolation unstated / confirmed isolated / confirmed not possible but insulated gloves + tester used as mitigation), where the third state should plausibly land at a *different* (likely lower) escalation than the first, since a real, stated mitigation exists. `ai/rule_engine/rules.py`'s current `_SAFETY_CRITICAL_FOLLOWUPS` treats any falsy/missing `power_isolated` identically. Worth designing for explicitly when Phase 5 replaces the placeholder — not a bug in the placeholder (it's not meant to be final), just a design note so the nuance isn't lost.

## Open Questions Carried from `prd.md`

- Minimum dataset size and expert-review process — **provisionally resolved 2026-07-19** (no supervisor available) at ~150–300 examples/class, standards-based review substitute — see `prd.md` §7. Confirm with supervisor when available.
- Acceptable false-negative rate for high-risk classes — **provisionally resolved 2026-07-19** at ≥95% recall on Professional Required/Dangerous — see `prd.md` §7. Confirm with supervisor when available.
